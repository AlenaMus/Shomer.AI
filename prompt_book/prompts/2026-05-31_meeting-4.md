# Session prompts — 2026-05-31 · Meeting 4 prep

**Date:** 2026-05-31
**Preparing for:** Meeting 4 (2026-05-31 — today)
**Session focus:** Low-Level Designs for the 3 core AI/ML modules (OCR Pipeline, Frontline Classifier, Context Agent).

---

## Turn-by-turn digest

### 1. LLD generation for 3 AI/ML modules
**Prompt (1-line restatement):** Produce full Low-Level Design documents for the OCR Pipeline, Frontline Classifier, and Context Agent modules — including Logger/Config/Metrics sections, Mermaid sequence diagrams, NFR test plans, and a dedicated TokenManager subsection for the Context Agent.
**Delivered:**
- `docs/design/ocr/design.md` — OCR Pipeline LLD (~310 lines): input validation, preprocessing params (adaptive threshold block_size=31/c=10, psm 6), `OcrResult` schema, `OcrBackend` + `ImagePreprocessor` + `TesseractRunner` class design, 2 sequence diagrams, CER < 15% test plan, failure modes, extraction seam.
- `docs/design/classifier/design.md` — Frontline Classifier LLD (~340 lines): DictaBERT sequence classification recipe (no QLoRA), calibration design (`ConfidenceCalibrator`), `OllamaClassifier` + `HuggingFaceClassifier` Protocol seam, export path discussion (HuggingFace direct recommended over GGUF for BERT encoder), 3 sequence diagrams, F1≥0.78 + ECE<0.10 test plans.
- `docs/design/context_agent/design.md` — Context Agent LLD (~430 lines): tool JSON schemas, Hebrew system+user prompt template, `LlmRouter` (GPT-4o-mini → Haiku fallback), `ContextAgent` class, Pydantic output validation, A/B experiment fields (`frontline_prediction`, `context_used`), full `§6.4 TokenManager` (Protocol, SQLite schema, UPSERT logic, Prometheus metrics, sequence diagram, `token_prices.yaml`), McNemar test plan for ΔFPR ≥ 15pp, 4 sequence diagrams.
- `docs/design/` directory structure created.
- `prompts/2026-05-31_meeting-4.md` — this file.

**Decisions / files of note:**
- No new `.decision.md` — all design choices flow from already-locked decisions in `architecture.decision.md`, `phase-2.decision.md`, `prd-enrichment.decision.md`.
- Noted in classifier LLD: DictaBERT-base should use `AutoModelForSequenceClassification` (not QLoRA / SFTTrainer) — existing `train_lora.py` targets generative models; a new `train_dictabert.py` is needed for Meeting 5. This is an implementation detail that follows from `D-Arch-Model`, not a new decision.
- `token_prices.yaml` carries `[למקור]` tag consistent with business plan convention — prices to be re-verified before Meeting 8.

---

## Open / carry-over to next session

- ⬜ `training/train_dictabert.py` — new script for DictaBERT sequence classification fine-tune (existing `train_lora.py` is for generative models). Needed for Meeting 5.
- ⬜ `training/calibrate.py` — fit isotonic regression on validation set after training. Needed for Meeting 5.
- ⬜ `server/app/context_agent/` directory and files — create the package skeleton (all module files are currently design-only).
- ⬜ `server/data/slang_lexicon.json` — Meeting 6 deliverable; ~200 entries of Israeli teen slang.
- ⬜ Open Q1 across all 3 LLDs: normalize `"none-offensive"` vs `"non_offensive"` label in `prompt.py`, `schemas.py`, `evaluate.py`.
- ⬜ Confirm Haiku 4.5 Anthropic API model ID (Q7 in context_agent LLD) before first integration test.
- 🟡 PRD v1.0 submitted to Meeting 4 — `docs/PRD.md` is complete. LLDs are the design-depth layer behind it.
- ⬜ Meeting 4 sign-off: get Dr. Segal's approval on Architecture B + RQ before any coding starts.

## Files touched this session

- `docs/design/ocr/design.md` — created (OCR Pipeline LLD)
- `docs/design/classifier/design.md` — created (Frontline Classifier LLD)
- `docs/design/context_agent/design.md` — created (Context Agent LLD, includes TokenManager §6.4)
- `prompts/2026-05-31_meeting-4.md` — created (this file)
