# Session prompts — 2026-06-04 (session 2) · Meeting 5 prep

**Date:** 2026-06-04
**Preparing for:** Meeting 5 (date TBD)
**Session focus:** What to present at Meeting 5 + how to demo the server skeleton and SDK → built a working Python demo CLI and fixed the Context Agent boot path.
**Note:** Second working session this date; the first (`2026-06-04_meeting-5.md`) covered WSL2+CUDA verification for Track A.

---

## Turn-by-turn digest

### 1. "Meeting 5 — what to present to Dr. Segal + what questions to ask?"
**Prompt:** Guidance on the Meeting-5 presentation and the questions to ask to continue the project.
**Delivered:**
- Reconciled `docs/meeting5_prep.md` (which assumes a trained DictaBERT + F1≥0.78 headline) with **actual state**: model NOT trained, Track C NOT started. Two-scenario presentation plan (trained vs. not-yet) keyed on the meeting date.
- Presentation structure (4 parts): academic gate (honest framing), live modular-server demo, the architectural contribution (composition root + G-03 triage fix), Meeting-6 roadmap.
- 5 questions for Dr. Segal: scope freeze (Q1 MoSCoW), single-child vs family (Q6), F1-gate validation, RQ3/RQ4 thesis-spine confirmation, Meeting-8 statistical bar.

### 2. "How can I show the server skeleton and SDK?"
**Prompt:** Concrete way to demonstrate the server skeleton + SDK at the meeting.
**Delivered:** Honest disk check — **server skeleton fully demoable** (composition root, modular packages, 172 tests, live `/classify`, Swagger `/docs`); **SDK does NOT exist as code** (`server/sdk/` = README placeholder; Track C never built). Gave 3 SDK options: present design only / quick Python CLI / real Kotlin `:sdk-cli`.

### 3. "Do option 2"
**Prompt:** Build the quick Python demo CLI.
**Delivered:**
- `scripts/dev_client.py` — httpx + stdlib terminal client; subcommands `health` / `info` / `classify` / `classify-image` / `demo` (`--json` mode). Forces UTF-8 stdout for Hebrew on Windows; ANSI colour on a TTY; friendly unreachable-server error.
- `scripts/golden_inputs.jsonl` — 10 curated Hebrew examples across all 5 labels (4 clean + 6 offensive), with `expected_category` + notes. Same schema as the planned Kotlin CLI → future cross-language parity test.
- **Verified live** against a running server: `health`/`info`/`classify` work; `demo` → **10/10 transport, 10/10 agreement, mean ~800ms** on the v1.0 stand-in; `--json` clean; unreachable path exits 1 gracefully.
- **Fixed a boot-blocking bug** in `server/app/main.py`: server crashed on startup with `CONTEXT_AGENT_ENABLED=true` (the default). `_build_context_agent()` was written against an imagined `LlmContextAgent(router, tools, settings)` API. Fixed 3 mismatches: `InMemoryTokenManager(settings)` → keyword args; `context_agent_history_turns` → `context_agent_max_history_turns`; and the `LlmContextAgent` constructor (the agent builds its own router+tools internally). Server now boots clean with CA enabled (Mock LLM fallback, no keys).

**Decisions / files of note:** `plan-docs/decisions/meeting-5-demo.decision.md` (D1 Python CLI over Kotlin SDK for the demo; D2 records the CA-wiring fix).

## Open / carry-over to next session
- ⬜ Track A still the critical path: data prep → `train_dictabert.py` → train → F1≥0.78. Demo agreement only becomes *meaningful* after this (flip `CLASSIFIER_MODEL_VERSION=v1.1-dictabert`).
- ⬜ Track C — the real Kotlin `:sdk-cli` is still the Meeting-5/6 demo deliverable; `dev_client.py` is the interim + parity reference.
- ⬜ Populate `server/.env` with real OpenAI/Anthropic keys to exercise the (now-bootable) Context Agent end-to-end.
- ⚠️ The CA-enabled boot path having never run before means the 172-test suite likely doesn't cover the composition root with CA on — worth an integration test (`SERVER-LIFESPAN-01`).
- ✅ Server skeleton + Python demo CLI ready for a live Meeting-5 demo.

## Files touched this session
- `scripts/dev_client.py` — created (Python terminal demo client)
- `scripts/golden_inputs.jsonl` — created (10 Hebrew golden examples)
- `server/app/main.py` — fixed `_build_context_agent()` (3 wiring bugs; server now boots with CA enabled)
- `plan-docs/decisions/meeting-5-demo.decision.md` — created
- `prompts/2026-06-04_meeting-5-2.md` — this file
