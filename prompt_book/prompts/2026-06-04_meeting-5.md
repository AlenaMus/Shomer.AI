# Session prompts — 2026-06-04 · Meeting 5 prep

**Date:** 2026-06-04
**Preparing for:** Meeting 5 (date TBD)
**Session focus:** Unblock Track A — verify WSL2+CUDA path for DictaBERT training on the RTX 5080 (sm_120 Blackwell).

---

## Turn-by-turn digest

### 1. Where are the training-data options & can we continue the plan?
**Prompt (1-line restatement):** Continue with project plan for training; show where the data-prep options are documented.
**Delivered:**
- Pointed at `docs/concepts/dictabert_data_techniques.md` (full technique catalog, 7 groups) — §11 is the locked Meeting-5 stack.
- Pointed at `docs/concepts/dictabert_classifier_architecture.md` §9 — the data contract the prep script must satisfy.
- Offered 3 next-step paths; user chose to verify WSL2 + CUDA before any data/training work (the Meeting-5 critical-path risk per CLAUDE.md).
**Decisions / files of note:** —

### 2. Verify WSL2 + CUDA for sm_120 Blackwell
**Prompt:** Confirm PyTorch can actually train on the RTX 5080 inside WSL2 before we sink time into data prep.
**Delivered:**
- WSL2 sanity: Ubuntu-24.04 default, `nvidia-smi` from inside WSL sees RTX 5080 (driver 591.86, CUDA 13.1 capability, 16 GB).
- **Confirmed the documented risk:** existing `torch 2.7.0.dev20250310+cu124` in `/home/alona/.local` lists supported archs only up to `sm_90`. Real CUDA matmul throws `no kernel image is available for execution on the device`.
- Created dedicated venv at `~/shomer-training-venv` (WSL native filesystem, not /mnt/c — for I/O speed).
- Installed `torch-2.11.0+cu128` + `torchvision 0.26.0+cu128` + `torchaudio 2.11.0+cu128` from the official `https://download.pytorch.org/whl/cu128` index (full CUDA 12.8 stack — cuDNN 9.19, cuBLAS 12.8.4, NCCL 2.28, Triton 3.6).
- **PASS:** new arch list = `['sm_75','sm_80','sm_86','sm_90','sm_100','sm_120']`. Real BF16 fwd+bwd test (10× 4096² matmul + backward) ran in 0.21 s using 0.17 GB VRAM. The Meeting-5 training path is alive.
**Decisions / files of note:** New `plan-docs/decisions/training-environment.decision.md` (logged below — captures venv location + torch build choices).

---

## Open / carry-over to next session

- 🟡 **Install remaining training deps into the venv** — `transformers`, `datasets`, `accelerate`, `peft`, `bitsandbytes`, `scikit-learn`, `huggingface_hub`. Recommend pinning into a new `training/requirements-wsl.txt`.
- ⬜ **Spawn `ai-researcher-developer` for `training/prepare_data_dictabert.py`** — task #19 (briefed). Inputs: architecture §9 (data contract) + techniques §11 (locked stack). Outputs: `data/train.jsonl`, `data/validation.jsonl`, `data/test.jsonl`, `data/class_weights.json`, `data/stylistic_eval.jsonl` (1040 held-out), `training/validate_splits.py`.
- ⬜ **Then:** write `training/train_dictabert.py` per architecture spec (MLP head + Focal Loss γ=2 + label smoothing ε=0.05 + AdamW + cosine LR + BF16 + 5 epochs).
- ⬜ **Train + evaluate** in WSL2 (~2–3 h on RTX 5080) → F1 ≥ 0.78 gate. If pass → flip `CLASSIFIER_MODEL_VERSION=v1.1-dictabert` in `server/.env`.
- ⬜ **Update CLAUDE.md** status table: "WSL2 + CUDA 12.8+ training environment NOT YET VERIFIED" → **VERIFIED 2026-06-04 (torch 2.11.0+cu128 / sm_120 confirmed)**.

## Files touched this session

- `prompts/2026-06-04_meeting-5.md` — created (this file)
- `plan-docs/decisions/training-environment.decision.md` — created (decisions D1 + D2 for venv location + torch build)
- `~/shomer-training-venv/` (WSL2, not in repo) — created; `torch 2.11.0+cu128` installed
