# Session prompts — 2026-06-06 · Meeting 6 prep

**Date:** 2026-06-06
**Preparing for:** Meeting 6
**Session focus:** Implement the full server functionality flow (every module
wired end-to-end, no DictaBERT training / no frontline-text changes), then a
Python debug SDK to exercise it.

## Turn-by-turn digest

### 1. Goal: full server flow for Meeting 6, then debug SDK
**Prompt (1-line restatement):** After Meeting 5, the Meeting-6 goal is to
implement the full server flow with all components working per the plan (no
DictaBERT training, no text-input changes), test the results, then build an SDK
to debug the server; create a plan, then tasks, then implement using
backend-developer + ai-researcher-developer agents.
**Delivered:**
- Ground-truth audit of current code vs. design: pipeline runs
  classifier→inline-triage→context_agent→in-memory audit; **missing**:
  Gatekeeper, Alerts (ALERT_DIRECT inert), standalone Triage, SqliteAuditStore,
  debug tooling.
- Three sprint decisions taken (see decision file).
- Plan written: `plan-docs/meeting6_server_flow_plan.md` (Phases 0/A/B/C/D).
**Decisions / files of note:**
- `plan-docs/decisions/meeting-6-server-flow.decision.md` — D1 LogNotifier
  default (+ FCM next step), D2 Python debug tools first (+ Kotlin :sdk-cli
  next), D3 optional `child_id` on /classify for real conversation history.
- `plan-docs/meeting6_server_flow_plan.md` — implementation plan.

### 2. Are prompts being updated per session?
**Prompt (1-line restatement):** Verify the prompts log is still being maintained per the CLAUDE.md convention.
**Delivered:** Audit of `prompts/` — 7 logs covering all working sessions from 2026-05-30 to today. Multi-session days correctly suffixed (`2026-06-04_meeting-5-2.md`). Today's file (this one) already in place from the earlier 09:16 session. Convention is being followed; days with no produced artifact (06-02, 06-05) correctly skipped.
**Decisions / files of note:** —

### 3. Create commercial-strategist agent
**Prompt (1-line restatement):** Create a new project-scoped agent owning business plan + profit + resource optimization + marketing + branding; wire in the `business-strategist` and `hebrew-latex-pdf` skills + check other existing skills to add.
**Delivered:**
- Surveyed the full skill catalog vs. the 5 requested responsibilities — picked **11 skills** total: `business-strategist`, `hebrew-latex-pdf`, `xlsx`, `pptx`, `deep-research`, `canvas-design`, `frontend-design`, `theme-factory`, `software-diagrams`, `product-manager`, `doc-coauthoring`. Skipped `brand-guidelines` (Anthropic-only), `docx`/`pdf` (load-on-demand), `internal-comms` (no recurring use yet).
- Created `.claude/agents/commercial-strategist.md` (~9 KB): YAML frontmatter + mission + responsibility→skill map + per-phase orchestration rules (business plan / profit / resources / marketing / branding) + handoff rules to `product-strategist`, `ai-researcher-developer`, `backend-developer`, `hebrew-ai-project-manager`.
- The new agent **takes over** the business-plan workflow that previously lived in `product-strategist` §8. `product-strategist` keeps its product↔plan consistency-check role.
**Decisions / files of note:** `plan-docs/decisions/commercial-strategist-agent.decision.md` — 4 decisions (D1 new agent vs. extend existing; D2 name `commercial-strategist`; D3 11-skill list; D4 boundary with `product-strategist`).

### 4. Implement the full server flow + debug SDK (Phases 0–D)
**Prompt (1-line restatement):** Proceed: build the full flow, then the SDK, create tasks, and implement with backend-developer + ai-researcher-developer agents.
**Delivered:**
- **Phase 0** — schema seam (`schemas.py`): optional `child_id`/`message_id`, `AlertRequest`/`AlertResult`.
- **Phase A** — 4 modules built in parallel by agents (3× backend-developer, 1× ai-researcher-developer): `triage/` (45 tests), `alerts/` w/ LogNotifier (105), `gateway.py` (37), `SqliteAuditStore` (23). Each Protocol + adapters + tests.
- **Phase B** — rewired `main.py` (v0.6.0-fullflow): SQLite store + retention sweeper, `ThresholdTriageEngine`, LogNotifier alert dispatch, `register_gateway()`, `child_id` conversation persistence, shared `_run_pipeline()`.
- **Phase C** — debug SDK: `dev_client.py replay`, `inspect_audit.py`, `load_test.py` + `tests/integration/test_full_flow.py` (7 tests, all 4 branches).
- **Phase D** — **384 fast tests pass**; live-verified on Ollama stand-in (demo 10/10, 6 sent + 2 rate_limited alerts, conversation + agent traces persisted, `/metrics` live). Report: `docs/meeting6_flow_test_report.md`.
- **4 bugs found in testing, all fixed** — missing `record_agent_trace`; `trace_id` not forwarded to CA; Windows cp1252 console dropping Hebrew `alerts.sent` log → dropped sent-alert audit rows (fixed by forcing UTF-8 stdio in `main.py`); `inspect_audit trace` dict-join crash.
**Decisions / files of note:** `docs/meeting6_flow_test_report.md`; all new modules under `server/app/{triage,alerts}/`, `server/app/gateway.py`, `server/app/audit_log/sqlite_adapter.py`.

### 5. Go over the gap MD and create missing agents + remove non-used agents
**Prompt (1-line restatement):** Act on `.claude/gaps.md` — build the missing project-scoped agents the gap analysis recommends and remove the duplicate non-customized agents.
**Delivered:**
- Verified all 6 candidate agents in `.claude/agents/` were byte-identical (`diff -q`) to user-global → safe to delete the 4 that have no customization plan.
- Surfaced via `AskUserQuestion`: scope = minimal vs. minimal+twins vs. full gaps.md action plan. User selected **full** ("Full gaps.md action plan (agents + skills)").
- **Removed 4 duplicates:** `ai-educator-architect.md`, `architecture-reviewer.md`, `project-organizer.md`, `project-planner.md` — they now inherit cleanly from `C:\Users\Dima\.claude\agents\` per the override rule.
- **Built 2 project twins:**
  - `.claude/agents/ai-researcher-developer.md` — encodes the locked DictaBERT architecture (MLP head + Focal γ=2 + ε=0.05 + AdamW + cosine LR + BF16 + 5 epochs), the WSL2 + cu128 training env at `~/shomer-training-venv`, the 184.3 M param + 6–8 GB VRAM real footprint discovery, the F1 ≥ 0.78 ship gate, the 5-label spelling locked at G-02, and the fallback chain. Declares 9 skills. Routes server integration to `backend-developer`.
  - `.claude/agents/backend-developer.md` — encodes the ports-and-adapters principle, the composition-root rule (concrete adapters constructed ONLY in `lifespan()`), the G-03 triage confidence-direction rule + mandatory 6-row polarity test, the audit_log adapter swap, the Windows UTF-8 stdio fix, the pytest-from-repo-root invariant. Declares 7 skills.
- **Promoted 2 skills:**
  - `.claude/skills/hebrew-latex-pdf/` — verbatim copy from user-global (no edits needed).
  - `.claude/skills/claude-api/` — copy from user-global + Shomer.AI addendum at the bottom of `SKILL.md` covering project-scoped Haiku 4.5 default (overriding the generic Opus 4.6 default), prompt caching mandatory, non-streaming, LlmRouter primary→fallback policy, and the 4 file paths to read before editing the Anthropic adapter.
- **Updated docs:** `.claude/agents/README.md` (project-scoped 2 → 4; user-global inherited 6 → 4; handoff matrix), `.claude/skills/README.md` (project-scoped count 20 → 22; user-global section trimmed), `.claude/gaps.md` (status note + Action plan items flipped to DONE; count tables updated).
**Decisions / files of note:** `plan-docs/decisions/agents-skills-cleanup.decision.md` — D1 remove 4 duplicates, D2 build 2 project twins (pulled forward from gap analysis's "Later"), D3 promote 2 skills, D4 documentation updates.

### 6. Friendly test console + Gemini + baseline accuracy eval
**Prompt (1-line restatement):** Build a friendly UI for manual full-flow testing (self-starting, health check, menu, result + alert + per-session audit/log files, hover-style descriptions); add Gemini support for the Context Agent; and run the OCR/text eval data through the pipeline to measure baseline accuracy as a PDF (no data changes / no training).
**Delivered:**
- **`scripts/test_console.py`** — interactive menu console: spawns its own isolated server, health-checks, per-session `audit.db` + `server.log` under `test-sessions/`, every classify shows classifier verdict + triage decision + CA verdict + alert sent; self-documenting menu + `?` full-flow help. Verified via `--selftest`. Also added `--child-id`/`--message-id`/`--trace-id` to `dev_client.py classify`.
- **Gemini** — `GeminiClient` (OpenAI-compat endpoint, reuses `openai` SDK), `gemini_api_key`/`gemini_model` settings, `_build_llm_clients` rewritten (priority Gemini>OpenAI>Anthropic; **fixed latent bug** of passing settings instead of key string), `.env.example` updated, 3 conformance tests. Decision: `plan-docs/decisions/gemini-context-agent.decision.md`.
- **Baseline accuracy eval** — `scripts/eval_accuracy.py` ran 200 text + 300 image samples through the live server. **TEXT 69.5 % / IMAGE 66.3 %** 5-class accuracy; **~80 % binary** offensive-vs-not on both; `hate` recall ~0.10 (collapses into abusive/non_offensive); **image ≈ text → OCR is not the bottleneck, the classifier is.** PDF: `docs/meeting6_accuracy_report.pdf`; summary: `docs/meeting6_accuracy_summary.md`; raw: `docs/accuracy_eval/results.json`. Strong quantified motivation for the DictaBERT fine-tune.
**Decisions / files of note:** `plan-docs/decisions/gemini-context-agent.decision.md`; the accuracy report PDF + summary.

## Open / carry-over to next session
- **Add `GEMINI_API_KEY=<key>` to `server/.env`** to activate the Gemini-backed Context Agent (wiring is done + tested; just needs the key).
- Backlog follow-ons: `M6-ALERTS-FCM` (real Firebase `FcmNotifier`), `M6-SDK-KOTLIN` (Gradle
  :sdk-cli), `/health` deep rollup, production JSON log renderer, G-04…G-14 reconciliation.
- After flow lands + tested: revisit DictaBERT training track (still the
  separate Meeting-5 critical path) and populate `server/.env` with real LLM keys.
- Consider updating `.claude/agents/product-strategist.md` §8 to explicitly route business-plan invocations to `commercial-strategist` (currently still says "invoke `business-strategist` skill" directly) — small docs edit, no rush.
- First real exercise of `commercial-strategist`: filling the `[למקור]` placeholders in `docs/business_plan/business_plan.tex` via `deep-research` + `business-strategist`.
- First real exercise of the new project-twin `ai-researcher-developer`: `training/prepare_data_dictabert.py` (briefed and unblocked) — the twin now encodes the locked architecture so the data-prep agent invocation no longer needs to re-read the architecture doc to find the contract.
- First real exercise of the new project-twin `backend-developer`: the `M6-ALERTS-FCM` Firebase notifier or the `/health` deep rollup — both are the right shape (Protocol+adapter, composition-root wiring, contract test) to verify the twin's guidance lands.

## Files touched this session
- `plan-docs/decisions/meeting-6-server-flow.decision.md` — created (earlier session)
- `plan-docs/meeting6_server_flow_plan.md` — created (earlier session)
- `prompts/2026-06-06_meeting-6.md` — created (earlier session) + appended turns 2–3 (this session)
- `.claude/agents/commercial-strategist.md` — **created** (new agent)
- `plan-docs/decisions/commercial-strategist-agent.decision.md` — **created** (D1–D4 for the new agent)
- `server/app/schemas.py` · `server/app/main.py` — modified (schema seam + full-flow wiring)
- `server/app/triage/`, `server/app/alerts/`, `server/app/gateway.py`, `server/app/audit_log/{sqlite_adapter,settings,retention}.py` — **created** (new modules)
- `server/tests/{triage,alerts,gatekeeper,audit_log,integration}/` — **created** (tests)
- `scripts/dev_client.py` (+replay) · `scripts/inspect_audit.py` · `scripts/load_test.py` — debug SDK
- `docs/meeting6_flow_test_report.md` — **created** (Phase D report) · `CLAUDE.md` — 2026-06-06 block + status row · `.gitignore` — server/data db rules
- `.claude/agents/{ai-educator-architect,architecture-reviewer,project-organizer,project-planner}.md` — **deleted** (duplicates of user-global)
- `.claude/agents/ai-researcher-developer.md` · `.claude/agents/backend-developer.md` — **rewritten as project twins** with Shomer.AI-specific guidance
- `.claude/agents/README.md` — updated (project-scoped 2 → 4; user-global inherited 6 → 4; handoff matrix)
- `.claude/skills/hebrew-latex-pdf/` · `.claude/skills/claude-api/` — **created** (promoted from user-global; claude-api has a Shomer.AI addendum)
- `.claude/skills/README.md` — updated (project-scoped 20 → 22; user-global section trimmed; gap pointer updated)
- `.claude/gaps.md` — updated (status note + Action plan items flipped to DONE; agent table updated; count tables updated)
- `plan-docs/decisions/agents-skills-cleanup.decision.md` — **created** (D1–D4 capturing this cleanup pass)
- `scripts/test_console.py` — **created** (interactive test console) · `scripts/dev_client.py` — `--child-id/--message-id/--trace-id` added
- `scripts/eval_accuracy.py` — **created** (baseline accuracy harness)
- `server/app/context_agent/clients/gemini_client.py` — **created** · `settings.py` + `main.py` `_build_llm_clients` — Gemini wiring + key-string bugfix · `server/.env.example` — Gemini keys
- `server/tests/context_agent/test_gemini_client.py` — **created** (3 tests)
- `docs/meeting6_accuracy_report.pdf` · `docs/meeting6_accuracy_summary.md` · `docs/accuracy_eval/results.json` — **created** (baseline: text 69.5% / image 66.3%)
- `docs/meeting6_manual_testing_guide.md` (+`.pdf`) — **created** (now leads with the interactive console) · `.gitignore` — `test-sessions/` added
