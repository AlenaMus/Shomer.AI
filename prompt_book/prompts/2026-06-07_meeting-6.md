# Session prompts — 2026-06-07 · Meeting 6 prep

**Date:** 2026-06-07
**Preparing for:** Meeting 6
**Session focus:** Housekeeping — compact the load-bearing `CLAUDE.md` handoff doc, and bring the prompt book up to date with the Meeting 4–6 agent work.

---

## Turn-by-turn digest

### 1. Compact CLAUDE.md to essentials
**Prompt (1-line restatement):** Rewrite `CLAUDE.md` to keep only important information, do compaction, ask if anything is unclear.
**Delivered:**
- Asked 3 clarifying questions via `AskUserQuestion`. Choices: **keep only the latest session block** (drop the four older 2026-05-24 → 06-04 blocks), **condense the process rules to bullets + pointers**, **back up the original first**.
- `CLAUDE.full.md` — created as a verbatim backup of the original (~600 lines).
- `CLAUDE.md` rewritten ~600 → ~185 lines. New structure: What this is → Directory map → Status table → Server behavior → Training env → Next priorities → latest session (06-07) → Plan/academic framing → REQUIRED rules → Conventions → How to resume.
- Folded still-live facts from the dropped blocks into the status table + three tight new sections (Server behavior to remember, Training env, Next session priorities); condensed decision-capture + prompts-log rules to bullets pointing at `_template.md` and existing decision files; removed the stale post-migration cleanup checklist.
**Decisions / files of note:** No `.decision.md` (documentation compaction, not a project decision). Flagged 2 judgment calls to the user: cutting the migration checklist, and keeping the 06-07 block despite minor redundancy with the new sections.

### 2. Update the prompt book with the new prompts + regenerate PDF
**Prompt (1-line restatement):** Update the existing prompt book with the new prompts and regenerate its PDF.
**Delivered:**
- Read all 7 session logs (2026-05-30 → 06-06) to extract the *significant* agent-orchestration prompts.
- `docs/prompt_book/prompt-book.md` — added **6 records (4–9)** in the existing 7-field Hebrew RTL format: (4) 10-LLD design package via 3× `product-strategist`; (5) architecture review + 3-blocker fix via `architecture-reviewer`; (6) modular server build via `ai-researcher-developer` + 2× `backend-developer`; (7) DictaBERT NN architecture lock via `ai-educator-architect`; (8) full server flow via 4 parallel agents; (9) Gemini CA + baseline accuracy + test console. `.md` 5.9 KB → 16.4 KB.
- `docs/prompt_book/prompt-book.pdf` — regenerated via `scripts/md_to_pdf.py` (138 KB → 279 KB, RTL preserved).
**Decisions / files of note:** Deliberately skipped pure-tooling turns (skill/agent creation, WSL2+CUDA verification, PDF re-renders) as agent *infrastructure* rather than significant project-AI uses; offered to add them if an exhaustive ledger is wanted.

### 3. Plan the train/validation/test dataset build for the DictaBERT classifier
**Prompt (1-line restatement):** Prepare a plan for training/validation/test datasets — review existing + downloadable sources, plan EN→HE translation (context-preserving) and synthesis, estimate how much data per class for good Hebrew accuracy and how long given the RTX 5080 / 64 GB, then modify the data for best classifier accuracy.
**Delivered:**
- 3 parallel `Explore` agents → full data inventory (SinaLab is minority-starved ~1,200 offensive; the 1,040 stylistic sentences are reusable as held-out eval; `train/val/test.jsonl`, `class_weights.json`, `prepare_data_dictabert.py`, `train_dictabert.py` all still missing).
- 3 clarifying questions via `AskUserQuestion` → see decisions D1/D2/D3 below.
- Approved plan written to `~/.claude/plans/prepare-plan-for-data-wiggly-storm.md`: per-class sizing math (lift hate/violence/porn to ~2k each → ~15–18k train), time estimates (GPU not the bottleneck — train run ~10–30 min; data prep/QA is the cost), staged Stage 0→4 execution, files to create, verification.
**Decisions / files of note:** `plan-docs/decisions/data-pipeline.decision.md` — **D1** Cloud Gemini for translation+synthesis · **D2** staged build (baseline on SinaLab+EDA first, expand only where weak) · **D3** single-message classifier scope only (conversational data is a later Context-Agent round). Noted: new prep script must use §9 severity priority `violence > hate > pornographic > abusive` (legacy `prepare_data.py` uses a different order).

---

## Open / carry-over to next session
- Unchanged from 2026-06-06 carry-over: add `GEMINI_API_KEY` to `server/.env`; backlog follow-ons (`M6-ALERTS-FCM`, `M6-SDK-KOTLIN`, `/health` deep rollup); DictaBERT training track is still the separate critical path — first action `training/prepare_data_dictabert.py`.
- Decide whether `CLAUDE.full.md` backup should be kept, gitignored, or deleted (git history already preserves the original).
- Optional: add the skipped tooling turns to the prompt book if an exhaustive agent ledger is desired.

## Files touched this session
- `CLAUDE.md` — rewritten/compacted (~600 → ~185 lines)
- `CLAUDE.full.md` — created (verbatim backup of original CLAUDE.md)
- `docs/prompt_book/prompt-book.md` — added records 4–9 (Meetings 4–6 agent work)
- `docs/prompt_book/prompt-book.pdf` — regenerated
- `prompts/2026-06-07_meeting-6.md` — created (this file)
