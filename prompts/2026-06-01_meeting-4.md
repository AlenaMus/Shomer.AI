# Session prompts — 2026-06-01 · Meeting 4 prep

**Date:** 2026-06-01 (session straddled 2026-05-31 → 2026-06-01)
**Preparing for:** Meeting 4 (date TBD — package ready for sign-off)
**Session focus:** Build the full pre-implementation design package — 10 LLDs, 144 tasks, architecture review, blocker fixes, terminal SDK runner, PDFs.

> **Note on dates:** the bulk of this session's work (turns 1–5 below) ran on 2026-05-31; turns 6–8 ran after the date rolled over. Filed under 2026-06-01 because that is the date of the session's substantive close. The existing `2026-05-31_meeting-4.md` records only the AI-core LLD sub-agent's output — this file is the full session audit trail.

---

## Turn-by-turn digest

### 1. Spawn product-manager + project-organizer agents → LLDs for 9 modules
**Prompt (1-line restatement):** Use product manager + project organizer agents to analyze the PRD and produce one Low-Level Design per module (Android client, SDK, Gatekeeper, OCR, DictaBERT classifier, Context Agent, Alerts, Triage, full Server). Ask first what is needed; JSON tasks per module; everything under a `design/` folder.
**Clarifications gathered:** in-process modules per PRD (modular monolith with clean Protocols) · `docs/design/<module>/` layout · rich JSON schema · LLDs include API contracts + internal design + Mermaid sequence + NFR + test plan + failure modes + deployment + logger + config + metrics. Plus: token manager for Context Agent; per-module logger/config/metrics.
**Delivered:**
- 9 `design.md` files at `docs/design/<module>/` — ~5,470 lines total, written by 3 parallel `product-strategist` agents (client-edge, AI-core, orchestration). Same 11-section template.
- Context Agent §6.4 TokenManager — Protocol + SQLite UPSERT schema + before_call/after_call + `token_prices.yaml` + Prometheus counters + sequence diagram.
- Surfaced gaps to fix later: DictaBERT training script gap (`train_lora.py` targets generative models; new `train_dictabert.py` needed for the encoder), `none-offensive` vs `non_offensive` label spelling, `OcrBackend` refactor to separate extraction from classification.
**Decisions / files of note:** No new `.decision.md` — everything flows from already-locked decisions in `architecture.decision.md`, `phase-2.decision.md`, `prd-enrichment.decision.md`.

### 2. OOP / isolation enhancement pass + JSON task backlogs
**Prompt:** Every component must be swappable — OCR engine, DictaBERT implementation, Context Agent — without changing the server. Each component has its own interface, isolated from the others. Strong OOP.
**Delivered:**
- `docs/design/README.md` (~17 KB) — project-wide ports-and-adapters principles, 5 swap scenarios, 5 OOP discipline rules, composition-root pseudocode, contract-testing strategy, module dependency table.
- Each of the 9 LLDs gained a new **§2.5 "Interface boundary & isolation guarantees"** naming the Port (Protocol) + ≥2 concrete adapters with one-line swap demos.
- `tasks_index.json` + 9 per-module `tasks.json` — 124 tasks (later 144) with rich schema; every module gets the mandatory cross-cutting tasks (`IF-01`, `CFG-01`, `LOG-01`, `MET-01`, `CT-01`, `UT-01`, `IT-01`, `DOC-01`, `FAIL-01`).
**Decisions / files of note:** Composition-root pattern locked: only `server/app/main.py` `lifespan()` constructs concrete adapters. The README is the source of record for what is allowed to import what.

### 3. Architecture review — verify LLD coverage + surface open questions
**Prompt:** Review the architecture design and charts and verify the LLD architecture contains all necessary functionality. Are there open questions to resolve before tasks execution?
**Delivered:**
- `docs/design/review.md` (486 L) via `architecture-reviewer` agent — coverage matrix against PRD §1-§15 + 3 Mermaid diagrams + locked decisions; 10 cross-cutting concerns (trace_id propagation, A/B switch, privacy, retention, cost, label consistency, confidence direction, health rollup, config composition, NFR test ownership) walked end-to-end.
- Verdict: **Conditional Ready · 3 Blockers · 7 Important · 5 Nice-to-have**.
- 3 Blockers identified: G-01 Audit Log has no standalone LLD · G-02 label spelling `none-offensive` vs `non_offensive` divergence · G-03 confidence-direction footgun in triage `_decide_inner`.
**Decisions / files of note:** review.md is treated as a live status board, updated whenever items resolve.

### 4. Resolve the 3 blockers
**Prompt:** Yes [fix the 3 blockers].
**Delivered:**
- **G-01** — new `docs/design/audit_log/` (758 L LLD + 14 tasks) via `product-strategist` agent. 5-table SQLite schema (`classifications`, `agent_traces`, `alerts`, `conversations`, `gold_set_metadata`) with Meeting-8 ΔFPR query columns (`gold_label`, `frontline_only_decision`, `context_agent_enabled`) baked in. WAL mode, `NullAuditStore` failure adapter, 7-day `RetentionSweeper`. `tasks_index.json` extended to 138; `SERVER-LIFESPAN-01` and `CTX-TOOLS-01` updated to depend on `AUDIT-IF-01`. README module index extended to 10 rows.
- **G-02** — canonical label locked to `non_offensive` (underscore). Direct edits to `PRD.md` §8.1, `classifier/design.md` §1/§2.2/§5.2/§5.3/§6.1/§8/§11 (10 surgical Edit calls), `classifier/tasks.json` (replace_all + one semantic flip on the legacy-normalization test acceptance criterion).
- **G-03** — triage `_decide_inner` rewritten with Step A normalization (`prob_offensive = result.confidence if result.is_offensive else 1 - result.confidence`) before Step B threshold routing. Decision Rules Summary updated. 6-row polarity test matrix added as a mandatory contract test in `tests/contracts/test_triage_engine_contract.py`.
- One cross-reference patch: audit_log/tasks.json cited a non-existent `ALERTS-PERSIST-01`; corrected to `ALERTS-DB-01`.
- `review.md` updated with a status banner + per-blocker ✅ RESOLVED notes.
**Decisions / files of note:** No new `.decision.md` — these are LLD-level fixes, not architectural reversals.

### 5. Add terminal SDK runner + server dev tools (Meeting 4 demo + Meeting 8 batch)
**Prompt:** Need ability to run SDK in terminal for server testing locally (without real clients), to demonstrate server capabilities. Add tasks for this — related to SDK and server testing/debug abilities.
**Delivered:**
- **3 SDK tasks** (`SDK-CLI-01/02/03`) — new Gradle `:sdk-cli` subproject wrapping `ShomerClient`. Subcommands `classify` / `classify-image` / `health` / `info` / `demo` / `batch`. Bounded-concurrency batch mode (`--parallel N`) for Meeting 8 gold-set runs.
- **3 server tasks** (`SERVER-DEV-CLI-01/02/03`) — `scripts/dev_client.py` (httpx CLI with `replay <trace_id>` regression mode), `scripts/inspect_audit.py` (read-only SQLite inspector), `scripts/load_test.py` (NFR validation with Markdown report).
- LLD additions: `sdk/design.md` §3.5 (CLI runner) + `server/design.md` §9a (developer tools & debug interfaces).
- `tasks_index.json` 138 → 144; sdk 13 → 16; server 17 → 20.
**Decisions / files of note:** Kotlin CLI's `demo` subcommand and Python `dev_client.py demo` share the same `golden_inputs.jsonl` schema → **cross-language wire-protocol contract test** (closes review.md G-05's blast radius even though G-05 itself remains formally open).

### 6. Generate PDFs for all design components
**Prompt:** Add PDF for all the design components you created.
**Delivered:**
- Minor direction-agnostic CSS tweak to `scripts/md_to_pdf.py` (`text-align: start`, `padding-inline-start`, `border-inline-start`) so the script renders both Hebrew RTL docs and English LTR design docs correctly without forking.
- 12 PDFs at `docs/design/{README,review}.pdf` and `docs/design/<module>/design.pdf` (~5.6 MB total). Mermaid diagrams render via the existing mermaid.js CDN injection + 25 s virtual-time-budget for Edge.
- Skipped: `tasks.json` / `tasks_index.json` (machine-readable structures, ugly as PDF).
- Re-run command: `python scripts/md_to_pdf.py docs/design/README.md docs/design/review.md docs/design/*/design.md`.
**Decisions / files of note:** —

### 7. Update CLAUDE.md + write this prompts log
**Prompt:** Update status of project for next session to continue from this.
**Delivered:**
- `CLAUDE.md` — added "Session update — 2026-05-31 → 2026-06-01" block; updated "Current status" table with a "Design package" row + date refresh to 2026-06-01; updated "How to resume next session" §5 to point to Meeting 4 sign-off and Sprint 1.
- `prompts/2026-06-01_meeting-4.md` — this file.
**Decisions / files of note:** —

---

## Open / carry-over to next session

- ⬜ **Meeting 4 sign-off** with Dr. Segal — Architecture B + RQ3 framing + 144-task backlog + 12 design PDFs. Decision to capture: confirmation that the 7 Important review-issues can be deferred to week 1 of implementation.
- ⬜ **Sprint 1 (Meeting 5)** — the 10 `*-IF-01` Protocol-definition tasks. Recommended first: `AUDIT-IF-01` (blocks `CTX-TOOLS-01`, `SERVER-LIFESPAN-01`, `ALERTS-DB-01`).
- ⬜ **G-04 port-naming reconciliation pass** (~30 min): pick `TriageEngine` / `NotificationChannel` / `ContextReasoner` / `TokenBudgetGuard` and grep-replace. Both name sets currently coexist in the LLDs.
- ⬜ **G-05** Android `ClassificationSource` (throws) vs SDK `ShomerApi` (returns `ShomerResult<T>`) error-model — pick one before `SDK-CLIENT-01` lands.
- ⬜ **G-06** PII scrub before LLM call — needs implementation owner (likely `context_agent`).
- ⬜ **G-07** Meeting 8 A/B evaluation procedure — needs a full doc.
- ⬜ **G-09** Slang Lexicon LLD — currently only referenced as `SlangDB` C4 box + `lookup_slang` Context-Agent tool; no schema, source, or update path defined.
- ⬜ **G-12** `/health` rollup missing OCR + Context Agent checks.
- ⬜ **G-14** Gold-set annotation procedure (partially answered by `AUDIT-GOLD-01`).
- ⬜ **Repo hygiene before Sprint 1:** recreate `server/.venv/`; re-open `android_client/` in Android Studio from the new path; consider initial commit + tag `v0.4-design-frozen` once Meeting 4 signs off.
- ✅ 3 review blockers resolved (G-01 audit_log LLD, G-02 label spelling, G-03 triage polarity).
- ✅ Backlog generated, all parses validated (144 tasks, 10 modules, cross-references checked).
- ✅ 12 design PDFs generated and verified.

## Files touched this session

**Created (new):**
- `docs/design/README.md` — ports-and-adapters principles
- `docs/design/review.md` — architecture review
- `docs/design/tasks_index.json` — 144-task flat index
- `docs/design/android_client/{design.md, tasks.json}`
- `docs/design/sdk/{design.md, tasks.json}`
- `docs/design/gatekeeper/{design.md, tasks.json}`
- `docs/design/ocr/{design.md, tasks.json}`
- `docs/design/classifier/{design.md, tasks.json}`
- `docs/design/context_agent/{design.md, tasks.json}`
- `docs/design/triage/{design.md, tasks.json}`
- `docs/design/alerts/{design.md, tasks.json}`
- `docs/design/server/{design.md, tasks.json}`
- `docs/design/audit_log/{design.md, tasks.json}` — new module
- 12 `*.pdf` siblings under `docs/design/` and each module folder
- `prompts/2026-06-01_meeting-4.md` — this file

**Edited:**
- `docs/PRD.md` — §8.1 label spelling
- `docs/design/classifier/design.md` — 11 surgical edits (label spelling + §11 Q1 resolution)
- `docs/design/classifier/tasks.json` — label spelling + legacy-normalization test flip
- `docs/design/triage/design.md` — §3 `_decide_inner` rewrite + §3 polarity matrix + §11 G-03 row
- `docs/design/sdk/design.md` — new §3.5 (CLI runner)
- `docs/design/sdk/tasks.json` — 3 new tasks (`SDK-CLI-01/02/03`)
- `docs/design/server/design.md` — new §9a (dev tools)
- `docs/design/server/tasks.json` — 3 new tasks (`SERVER-DEV-CLI-01/02/03`)
- `docs/design/audit_log/tasks.json` — `ALERTS-PERSIST-01` → `ALERTS-DB-01` fix
- `scripts/md_to_pdf.py` — direction-agnostic CSS tweak
- `CLAUDE.md` — Current status table + Session update block + How-to-resume tail
