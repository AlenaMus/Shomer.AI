# Session prompts — 2026-06-03 · Meeting 5 prep

**Date:** 2026-06-03
**Preparing for:** Meeting 5 (date TBD; ~7-14 days from Meeting 4 sign-off)
**Session focus:** Build the modular server pipeline end-to-end, then lock the DictaBERT classifier neural-network architecture and the data-preparation strategy.

---

## Turn-by-turn digest

### 1. "Where from we should continue on Meeting 4 preparations?" → pivoted to Meeting 5
**Prompt (paraphrased):** Status check — what's left for Meeting 4. User clarified the question is actually Meeting 5 ramp: how to proceed with server implementation + SDK.
**Delivered:**
- Status assessment: PRD + design package + architecture diagrams + business plan ready; meeting agenda stale; nothing committed yet
- 3-track Meeting-5 plan written to `docs/meeting5_prep.md` (270 lines, Hebrew RTL) + PDF: Track A (DictaBERT training, academic gate F1≥0.78), Track B (server modular refactor), Track C (SDK + Kotlin CLI for demo)
- Critical-path Gantt + risks + presentation outline + open questions to close

**Decisions / files of note:** No new decision file; the agreed split lives in `docs/meeting5_prep.md`.

### 2. "Can we start building the server modules: classifier, OCR, Context Agent — using backend-developer + ml-developer in parallel"
**Prompt:** Build classifier (DictaBERT — needs to be downloaded with Hebrew support), OCR (already downloaded — wrap as module), Context Agent (full skeleton). Use parallel agents now that we have the Protocol interfaces.
**Delivered:**
- Pre-flight: venv verified functional, Ollama stand-in smoke-tested, `.gitignore` augmented, baseline tag `v0.4-design-frozen` committed (`a52f7c8`)
- Foundation (by me, ~30 min): `requirements.txt` expanded; `.env.example` rewritten; `schemas.py` extended with 6 internal value types (`ClassificationResult`, `OcrResult`, `TriageDecision`, `ContextDecision`, `HealthState`, `ModuleHealth`); 4 Protocol files written (`classifier/`, `ocr/`, `context_agent/`, `audit_log/`); legacy `classifier.py` → `_legacy_classifier.py` (git rename)
- 3 parallel implementation agents:
  - `ai-researcher-developer` → `server/app/classifier/` (~2150 lines, 57 tests): Ollama + HuggingFace + Stub adapters, ConfidenceCalibrator, 6 Prometheus metrics. DictaBERT base downloaded (~708 MB).
  - `backend-developer` → `server/app/ocr/` (~1400 lines, 45 tests): TesseractOcrBackend (extraction-only, real Tesseract verified) + StubOcrBackend + ImagePreprocessor (pure numpy, no OpenCV).
  - `backend-developer` → `server/app/context_agent/` (~2350 lines, 70 tests): LlmContextAgent + 3 LLM clients (Mock + OpenAI + Anthropic) + LlmRouter with failover + 3 tools + SqliteTokenManager + token_prices.yaml.
- `InMemoryAuditStore` stop-gap at `server/app/audit_log/in_memory_adapter.py` so Context Agent's `read_history` tool has something to talk to until `SqliteAuditStore` lands.
- `main.py` rewritten as composition root: every adapter swap is one env-var flip.
- End-to-end smoke test PASSING: `POST /classify "שלום עולם"` returns `non_offensive, confidence=0.95` via the real Ollama Qwen stand-in through the new `TextClassifier` Protocol.
- Total **172 tests passing** (167 fast + 5 slow that exercise the HF download path).
- Commits: `55527ff` (3 modules) → `2a14ab5` (main.py wiring).

**Decisions / files of note:** No new decision file. The composition-root pattern + Protocol-only cross-module imports are now real, not just docs.

### 3. "We don't have enough data to train DictaBERT — need SinaLab + generated data. Review architecture before training. How to prepare data?"
**Prompt:** Confirm SinaLab download is needed; understand DictaBERT as classifier; plan data prep.
**Delivered:**
- Data audit: SinaLab not downloaded; `data/ocr_validation/sentences.jsonl` already has **1040 labeled Hebrew sentences** across 4 styles (clear, code-switching, poor-spelling, children-mistakes) — surfaced as a stylistic robustness holdout
- DictaBERT-as-classifier teaching: BERT topology (12 encoder layers + [CLS] + linear head), pretrained encoder + RANDOMLY-INITIALIZED classification head, why full fine-tune (110M fits in VRAM, no QLoRA needed)
- Sketch of `prepare_data_dictabert.py` (label mapping, stratified split, class weights via `sklearn.compute_class_weight`)
- Plan: 3 sub-steps (inspect SinaLab → prepare data → review architecture interactively) → then train_dictabert.py + 2-3h training in WSL2

### 4. "Plan the full NN architecture carefully — do we have it? Use ai-educator-architect + ai-researcher-developer"
**Prompt:** Plan the network architecture (DictaBERT + new layers) for offensive classification carefully. Confirm we don't already have it. Set up the two-agent flow.
**Delivered:**
- Confirmed: classifier LLD only has basic hyperparams (lr, batch, epochs) — NOT a layer-by-layer architecture
- Spawned `ai-educator-architect` to produce **`docs/concepts/dictabert_classifier_architecture.md`** (903 lines, Hebrew RTL, full 12-section design + bibliography). Locked decisions:
  - **Architecture**: DictaBERT-base (fully fine-tuned topology) + **MLP head** `[CLS] → Dropout(0.1) → Linear(768→256) → GELU → Dropout(0.1) → Linear(256→5)`. ~198K new params on top of ~110M total.
  - **Loss**: Focal Loss (γ=2.0) with built-in `alpha=class_weights` — single mechanism handles imbalance + hard-example focus. Compared against Vanilla / Multi-task / Hierarchical / DAPT options.
  - **Training**: AdamW + cosine LR (warmup_ratio=0.1) + lr=2e-5 + batch=32 + 5 epochs + early-stopping(patience=2) + BF16 + seed=42; fits ~5-6 GB VRAM.
  - **Fallback chain**: MLP → Multi-task → DictaBERT-large → DAPT.
  - **§9 data contract**: 3 JSONL files + `class_weights.json` + `stylistic_eval.jsonl` (1040 OCR sentences held out), no-leakage validator script as DoD.
- Data-prep agent (task #19) briefed and unblocked, but NOT spawned this session — user signaled the session is ending.

**Decisions / files of note:** The architecture choices are documented in the .md doc; no separate .decision.md (the doc IS the decision record, with rationale + alternatives + literature citations).

### 5. "What ways do we have to change data for best results — accurate and fast?"
**Prompt:** Comprehensive treatment of data-side techniques for both accuracy AND speed.
**Delivered:**
- Inline taxonomy: 5 groups of techniques (pure wins, big accuracy levers, augmentation, stratification/robustness, inference speed) with concrete trade-offs and expected ΔF1
- Cheap-wins stack recommendation for Meeting 5

### 6. "Summarize all techniques, explain each, verify safety, can we modify later?"
**Prompt:** Write a permanent reference doc covering every technique, combination-safety analysis, modification path.
**Delivered:**
- **`docs/concepts/dictabert_data_techniques.md`** (~500 lines, Hebrew RTL, full PDF) — 11-section reference:
  - 7 technique groups (A: cleaning, B: efficiency, C: class-balance, D: splits, E: augmentation, F: deferred, plus combination-safety)
  - §8 combination-safety analysis: 4 known stacking risks (over-correction of imbalance, compounding softening of Focal+Smoothing, augmentation distribution shift, lossy preprocessing) with mitigations
  - §9 `DataPrepConfig` schema for future modification (every knob is parameterized)
  - §11 confirmed Meeting-5 stack ("option 2"): A1-A7 + B1-B3 + C2 (Focal+weights) + C4 (smoothing ε=0.05) + D1-D4 + E1-E3 capped at 2× on minorities. Skip C1 alone, C3 (fallback), E4 (weak Hebrew), F1-F8 (Meeting 6-8).
  - Extension path table: Meeting 5 → 6 → 7 → 8 with what to add at each step
- Commit `b8da7ff` — both reference docs + PDFs

**Decisions / files of note:** Combination-safety is a real concern — documented mitigations so the data-prep agent doesn't accidentally compound techniques. The doc explicitly recommends ε=0.05 (not 0.1) for label smoothing when Focal Loss is also used.

### 7. "Update CLAUDE.md once you finish — for next session continue"
**Prompt:** Session wrap. Set up next-session continuation.
**Delivered:**
- This file (`prompts/2026-06-03_meeting-5.md`)
- CLAUDE.md updated: "Current status" date refreshed to 2026-06-03; new "Server (Python)" + "DictaBERT classifier — design" rows added; new "Session update — 2026-06-03" block with all 7 turns summarized; "How to resume next session" §5 rewritten to point at task #19 (data-prep agent)

---

## Open / carry-over to next session

- ⬜ **First action**: spawn `ai-researcher-developer` for `training/prepare_data_dictabert.py` (task #19 already briefed; reads architecture §9 + techniques §11)
- ⬜ Verify WSL2 + CUDA 12.8 + PyTorch sm_120 wheels work (`python -c "import torch; print(torch.cuda.is_available())"`) — the single-point-of-failure for Track A
- ⬜ Write `training/train_dictabert.py` per locked architecture
- ⬜ Run training in WSL2 (~2-3 hours on RTX 5080); F1 ≥ 0.78 gate
- ⬜ Populate `server/.env` with real `OPENAI_API_KEY` + `ANTHROPIC_API_KEY` to enable Context Agent end-to-end (Mock fallback currently)
- ⬜ **Track C**: start SDK + Kotlin CLI in parallel (for Meeting-5 live demo)
- ⬜ Reconciliation pass G-04 (port naming) — ~30 min, worth doing before Sprint 2 lands
- ⬜ The other 6 Important review-issues (G-05, G-06, G-07, G-09, G-12, G-14) — can be addressed in parallel
- ⬜ Eventually: SqliteAuditStore replaces InMemoryAuditStore
- ✅ Modular server pipeline alive (172 tests + smoke test passing)
- ✅ DictaBERT classifier neural-network architecture locked (MLP head + Focal Loss)
- ✅ Data-prep techniques + combination-safety analysis written

## Files touched this session

**Created:**
- `docs/meeting5_prep.md` + `.pdf` — 3-track Meeting-5 plan
- `server/app/{classifier,ocr,context_agent,audit_log}/__init__.py` + `protocol.py` — 8 Protocol foundation files
- `server/app/classifier/` — 9 source + 7 test files (OllamaDictaBertClassifier, HuggingFaceClassifier, StubClassifier, ConfidenceCalibrator, settings, logging, metrics, README)
- `server/app/ocr/` — 9 source + 7 test files (TesseractOcrBackend, StubOcrBackend, ImagePreprocessor, TesseractRunner, settings, logging, metrics, README)
- `server/app/context_agent/` — 23 source + 8 test files (LlmContextAgent, LlmRouter, MockLlmClient, OpenAiClient, AnthropicClient, 3 tools, SqliteTokenManager, InMemoryTokenManager, prompt, output_parser, settings, logging, metrics, token_prices.yaml, README)
- `server/app/audit_log/in_memory_adapter.py` — stop-gap until SqliteAuditStore
- `server/data/slang_lexicon.json` — 10-entry Hebrew slang seed (for `lookup_slang` tool)
- `server/conftest.py`, `server/pytest.ini` — test infrastructure
- `docs/concepts/dictabert_classifier_architecture.md` + `.pdf` — 903 lines, Hebrew RTL, locked NN design
- `docs/concepts/dictabert_data_techniques.md` + `.pdf` — ~500 lines, Hebrew RTL, technique reference
- `prompts/2026-06-03_meeting-5.md` — this file

**Edited:**
- `server/app/main.py` — full rewrite as composition root
- `server/app/schemas.py` — extended with 6 internal pipeline value types
- `server/requirements.txt` — expanded for new modules
- `server/.env.example` — every key the new modules read
- `.gitignore` — added `.claude/`, `server/models/`, `server/.env`, intermediate `.html` artifacts, OCR validation logs
- `CLAUDE.md` — Current status table + Session update block + How-to-resume tail

**Renamed (git mv):**
- `server/app/classifier.py` → `server/app/_legacy_classifier.py`
