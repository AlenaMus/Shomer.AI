# Session prompts — 2026-06-07 · Meeting 6 prep

**Date:** 2026-06-07
**Preparing for:** Meeting 6 (continued — meeting itself hasn't happened yet per the 2026-06-06 file framing)
**Session focus:** Two new project-scoped agents — `android-developer` (real-client rewrite is now active work) and `project-manager` (whole-project coordinator across the 6 specialist agents).

## Turn-by-turn digest

### 1. Why no android-developer agent in project scope?
**Prompt (1-line restatement):** Asked why no `android-developer` agent exists at project scope despite `.claude/skills/android-developer/` being project-scoped.
**Delivered:** Brief explanation citing `.claude/gaps.md` ("Defer until Android work is active") + `.claude/agents/README.md` handoff matrix ("No agent yet — invoke the android-developer skill from the main thread. Worth creating if Android work picks up"). Offered to build the agent when the rewrite from POC to real client starts.
**Decisions / files of note:** — (no files produced; this was the lead-in to turn 2)

### 2. android_client is POC — need a dedicated agent for the real-client rewrite + add a project-manager
**Prompt (1-line restatement):** Confirmed the Android POC is being rewritten to the real client app; also asked for a project-manager / Hebrew project-manager agent that "manages the whole work."
**Delivered:**
- Surveyed current POC state: `com.dima.offensivehebrew` namespace, Compose `data/ui/viewmodel` layout, `Camera`+`Internet` perms, Retrofit `ApiService` + DataStore `SettingsRepository`. No `android_client/design.md` yet.
- Surfaced scope choice via `AskUserQuestion`: (a) one combined `project-manager` vs two split agents vs Hebrew-only; (b) build android-developer in the same pass vs defer. User picked **combined project-manager + build both agents now**.
- **Built `.claude/agents/android-developer.md`** (fresh — no user-global twin): encodes the POC → `com.shomer.client` rewrite plan with Gradle product flavors (`poc` + `client`) and the mandatory APK uninstall step; the LAN networking matrix (emulator `10.0.2.2:8000` vs phone `<PC-LAN-IP>:8000` + firewall rule); cleartext-HTTP `network_security_config.xml`; FCM receiver plan for `M6-ALERTS-FCM`; the boundary against `backend-developer` (server side) and Track-C `:sdk-cli` (server/sdk's deliverable, consumed not built here); locked classifier label spelling on the wire. Declares 5 skills: `android-developer`, `software-architect`, `software-diagrams`, `frontend-design`, `pdf`.
- **Built `.claude/agents/project-manager.md`** (fresh — no user-global twin; combined English + Hebrew + handoff orchestration): owns whole-project coordination; picks `hebrew-ai-project-manager` skill for Hebrew Dr. Segal prep + implementation summaries, `project-organizer` for English sprint + backlog, `project-planner` for risk + estimation, `software-diagrams` for Gantt / dependency / burndown, `pdf` for compile, `doc-coauthoring` for multi-section synthesis. Routing matrix to the 5 specialist agents. **Hard "always invoke a skill, never freehand" rule** + always-ask-for-meeting-date discipline. Supersedes the gap-analysis "don't create `hebrew-ai-project-manager-agent`" decision.
- **Updated `.claude/agents/README.md`**: project-scoped 4 → 6; new sections for both agents; handoff matrix updated to route Android client work + whole-project coordination to the new agents; agent → skill binding overview extended; `project-organizer` + `project-planner` re-described as indirect-via-PM (no longer direct from main thread).
- **Updated `.claude/gaps.md`**: agent count 4 → 6 (total 10 available); 2026-06-07 status note added at top; "Agents missing entirely" section flipped — `android-developer-agent` from "defer" to ✅ DONE; `hebrew-ai-project-manager-agent` from "don't create" to ✅ SUPERSEDED by combined `project-manager`.
**Decisions / files of note:**
- `plan-docs/decisions/android-developer-agent.decision.md` — D1 build now (real-client work active), D2 5-skill set, D3 boundary against `backend-developer` + Track-C `:sdk-cli` (consume not author the SDK), D4 Gradle product flavors `poc` + `client` for side-by-side install during transition.
- `plan-docs/decisions/project-manager-agent.decision.md` — D1 one combined agent (not two), D2 supersedes gap-analysis "don't create" decision, D3 7-skill set, D4 hard "no freehand" boundary against the 5 specialists.

### 3. Update skills README after the agent changes
**Prompt (1-line restatement):** Asked whether `.claude/skills/README.md` was updated alongside `.claude/agents/README.md` after the 06-07 agent additions.
**Delivered:**
- It hadn't been. Updated `.claude/skills/README.md`:
  - **User-global table:** pulled `project-organizer`, `project-planner`, and `project-manager` (skill) out of the catchall "Generic tooling" line into their own rows describing that they're now invoked via the `project-manager` *agent* (direct main-thread invocation discouraged). Updated `doc-coauthoring` to note dual invocation by both `commercial-strategist` and `project-manager` agents. Annotated `canvas-design` + `theme-factory` with `commercial-strategist` invocation.
  - **"How skills are invoked"** section: added a new **"Agent → skill bindings (project-scoped agents)"** block mirroring the binding map in `.claude/agents/README.md` (all 6 agents and their declared skill lists). Cross-links to the agents README.
  - **Gap analysis section:** added a 2026-06-07 line noting that no skill promotions happened this pass but 6 user-global skills moved from direct-main-thread to agent-invoked.
**Decisions / files of note:** —

### 4. Which server modules are still mocks — and implement the real gap
**Prompt (1-line restatement):** Plan-mode: which server modules are now mocks, what's needed for full operation + testing; then "overview the design for each module and have a concrete plan of implementation if there is any gap"; approved → implement.
**Delivered:**
- Plan-mode inventory (3 parallel Explore agents over `server/app/*`, `docs/design/*`): 7 of 10 modules are real; the "mocks" resolve into **train-only** (classifier — Ollama stand-in; real `HuggingFaceClassifier` wired, awaits checkpoint + `CLASSIFIER_MODEL_VERSION=v1.1-dictabert`), **config-only** (context_agent — `MockLlmClient` only fills empty key slots), and **one code gap: alerts/FCM**. Noted review.md G-04 (port naming) + G-12 (health probes) appear already satisfied in code. Plan file: `~/.claude/plans/implementation-of-server-modules-snappy-bird.md`.
- **Implemented `M6-ALERTS-FCM` (server side).** Real `FcmNotifier.send_alert()` per LLD §3: idempotency → resolve sender (lazy idempotent firebase init; graceful "FCM not configured" when lib/creds absent) → rate-limit → Hebrew FCM `Message` (9-field `data` payload + high-priority `AndroidConfig`) → exponential-backoff retry → `LocalRetryQueue` fallback → best-effort audit callback; never raises. Added a `send_fn` injection seam so the retry/queue/rate-limit logic is unit-tested without `firebase-admin` installed.
- `main.py` `lifespan()` now selects channel by `ALERTS_CHANNEL` (`fcm`/`stub`/`log`; default `log` unchanged). `requirements.txt` += `firebase-admin` (opt-in). `.env.example` Alerts section corrected (`ALERTS_CHANNEL`, `FCM_SERVICE_ACCOUNT_PATH`, real `ALERTS_` knobs; dropped stale `FCM_SERVICE_ACCOUNT_JSON`/`ALERTS_MAX_PER_MINUTE`).
- Replaced skeleton `test_fcm_stub.py` → `test_fcm_notifier.py` (not-configured path + configured path: success, 3×-fail→queued, rate-limited, idempotency, audit callback). Cleaned stale "backlog/not-enabled" doc comments in `__init__.py`/`protocol.py`.
- **Tests: 399 passed, 5 skipped** (5m25s; up from 387 — the 5 skips are HF classifier tests awaiting a checkpoint). Server venv at `server/.venv`, run from repo root.
**Decisions / files of note:**
- `plan-docs/decisions/alerts-fcm.decision.md` — D1 build FCM now (last server stand-in), D2 injectable `send_fn` seam + lazy import, D3 `ALERTS_CHANNEL` selection in composition root, D4 `firebase-admin` opt-in + bare `FCM_SERVICE_ACCOUNT_PATH` per LLD §6.

### 5. Plan + start the real monitoring app (child capture + parent review/react)
**Prompt (1-line restatement):** `/plan` with the architect + android-developer agents — design and start implementing a **real** Android app that tracks messages shown in *other* apps' chat/post windows and sends them to the server for Hebrew bullying classification + notification; clarified: child-side passive monitoring + parent-side receive/review/react for "unknown but may be offensive" borderline cases; alerts delivered **once a day**.
**Delivered:**
- Two specialist designs (background agents): `android-developer` produced the on-device capture plan (**`AccessibilityService`** primary reader of other apps' node trees, **MediaProjection + ML Kit OCR** fallback, foreground service, on-device pre-filter, encrypted offline buffer, batch `MonitorUploader`); `ai-educator-architect` produced the system architecture (reuse `_run_pipeline` verbatim; new ports `DedupStore`/`FlaggedEventStore`/`IdentityStore`/`DigestScheduler`; pairing + device-token auth; classify-and-discard privacy model).
- Locked 4 product decisions via `AskUserQuestion`: parent surface = **web dashboard + Android parent-mode, both in parallel**; monitor scope = **inbound + outbound**; parent react actions = **acknowledge + label offensive/not + severity**; build scope = **full MVP to working demo**. Then a 5th: **alerts once-a-day digest** (not per-event push) → added `DigestScheduler` port + `GET /v1/parent/digests/{date}`.
- Approved plan written to `~/.claude/plans/linked-yawning-sifakis.md` (S0→S6 phased, server+Android interleaved, demoable each phase).
- **Started the build.** Wrote 3 decision docs (S0). Launched `backend-developer` (background) to build **S1 server ingest**: new `dedup/`, `flagged/`, `monitor/` packages (each Protocol + ≥2 adapters) + `MonitorEvent`/`MonitorBatch*`/`FlaggedEvent` schemas + `POST /v1/monitor/events` → `MonitorIngest` fanning each event into the existing pipeline + `lifespan()` wiring + tests. Reuses the existing pipeline (no new classification path); borderline (`ESCALATE_TO_CA`/`REVIEW_NEEDED`) → flagged `status="review_needed"` = the parent's review queue.
**Decisions / files of note:**
- `plan-docs/decisions/monitor-architecture.decision.md` — D1 reuse pipeline + orchestrator, D2 new ports/adapters, D3 fast-ack + daily-digest cadence, D4 volume-reduction stack, D5 monitor raises classifier bar (gate v1.1 flip on a monitor-realistic slice).
- `plan-docs/decisions/privacy.decision.md` — D1 inbound+outbound scope, D2 classify-and-discard + deny-list, D3 device-token auth/TLS/at-rest/PII-scrub, D4 dual consent + non-dismissible indicator.
- `plan-docs/decisions/parent-surface.decision.md` — D1 one codebase two roles + web+Android both, D2 once-a-day digest, D3 react actions (label feeds DictaBERT training).

### 6. Implement S2 Identity & Auth
**Prompt (1-line restatement):** Build S2 (Identity & Auth) server side per approved plan `linked-yawning-sifakis.md` — child/parent identity, device-token auth, pairing OTP, Gatekeeper auth middleware, monitor endpoint authorization. From 454 passed, keep suite green.
**Delivered:**
- **`server/app/identity/` package** — new module following the canonical Protocol+adapters pattern:
  - `protocol.py`: `IdentityStore(Protocol)` + 3 frozen value types (`ChildRecord`, `PairingCode`, `DeviceContext`). Methods: `register_parent`, `authenticate_parent`, `issue_child`, `list_children`, `create_pairing_code`, `redeem_pairing_code`, `authenticate_device`, `parent_for_child`, `health`.
  - `sqlite_adapter.py`: `SqliteIdentityStore` — 4 tables (`parents`, `parent_tokens`, `children`, `pairing_codes`, `device_tokens`); WAL + asyncio.Lock + busy_timeout; `initialize()`/`close()` lifecycle.
  - `in_memory_adapter.py`: `InMemoryIdentityStore` — plain dicts, no I/O, for tests.
  - `settings.py`: `IdentitySettings` (env prefix `IDENTITY_`): `backend`, `db_path`, `device_token_ttl_days`, `pairing_code_ttl_s`, `busy_timeout_ms`.
  - `router.py`: 5 endpoints: `POST /v1/parent/register` (unauthed), `POST /v1/parent/children` (parent-authed), `GET /v1/parent/children`, `POST /v1/parent/pairing-code`, `POST /v1/pair` (unauthed bootstrap).
  - `__init__.py`: exports Port+types + adapters+settings + `register_identity`.
- **`gateway.py`**: Added `DeviceAuthMiddleware` + `_AUTH_ALLOWLIST`. Middleware runs after TraceId (so trace_id exists), before rate-limit in the execution path; registered in step 3b of `register_gateway`. Resolves Bearer token → `request.state.device_context`; content-blind (imports `IdentityStore` Protocol only). Fail-open on missing identity store.
- **`monitor/router.py`**: Added `_enforce_child_auth()` helper. Enforces: identity store present → 401 if no device_context; 403 if role != "child"; 403 if child_id mismatch. Auth is enforced in the router (not middleware) because the child_id match requires reading the request body. Removed `# No auth in S1` note.
- **`main.py`**: Added `_build_identity()` builder + wired `app.state.identity` in `lifespan()`. Identity store closed on shutdown. `register_identity(app)` called after `register_monitor`. Version bumped to `0.6.1-identity`.
- **`server/tests/identity/`** — 34 new tests:
  - `test_identity_store.py` (parametrized over InMemory+SQLite): uniqueness, UUID format/no-PII, list_children, parent_for_child, expired TTL, single-use code, full pairing→device-token→authenticate_device round-trip, unknown token → None, health.
  - `test_auth_middleware.py`: no token → 401; mismatched child_id → 403; valid token matching child_id → 202; allowlisted endpoints (/health, /v1/pair, /classify) open; full HTTP pairing flow end-to-end.
- **`server/tests/monitor/test_monitor_ingest.py`**: Updated 2 TestClient tests for S2 auth: `test_monitor_endpoint_via_testclient` now goes through the full pairing flow to mint a device token + includes it as a Bearer header. `test_monitor_endpoint_empty_child_id_returns_400` sets `app.state.identity = None` to disable auth and isolate the validation check.
- **Test result: 488 passed, 5 skipped, 0 failed** (up from 454 + 5 skipped).
**Decisions / files of note:**
- Auth enforcement location: in the monitor **router** (not middleware) — because the child_id match requires reading the batch body, which is not available to the Gatekeeper middleware. Middleware stays content-blind (sets `device_context`); router enforces ownership.
- `plan-docs/decisions/privacy.decision.md` D3 (auth section) was the pre-existing lock for this decision.

## Open / carry-over to next session
- **First real exercise of `project-manager`** — invoke it for the Meeting-6 prep doc (ask for meeting date first, per its always-invoke-a-skill rule), then route the Meeting-7 sprint plan through it.
- **First real exercise of `android-developer`** — the package rename + product-flavors PR. Keep it on a `feature/package-rename-com-shomer-client` branch so the rename diff is reviewable independently of any new-screen work. Surface the APK uninstall step to the user explicitly when the PR lands.
- **`android_client/design.md` does not exist yet** — the 11-section LLD template lives in `docs/design/README.md`; the `android-developer` agent's reading list flags this as a near-term carry-over. Worth landing the LLD before the rename PR so the rewrite has a written contract.
- **`docs/risks.md` does not exist yet** — the `project-manager` agent's mission step #4 references it as the single living risk register. First risk-tracking invocation creates the file.
- `M6-ALERTS-FCM` **server side is now DONE** (real `FcmNotifier` + `ALERTS_CHANNEL` selection, 399 tests green). Remaining FCM work is the **Android side** (`ShomerFcmService` receiver + Room dedup + deep-link nav, owned by `android-developer`) and **ops** (a real Firebase project + service-account JSON) before a live device-push end-to-end test.
- Pre-existing carry-overs unchanged from 2026-06-06: populate `server/.env` with real LLM keys; DictaBERT training track unblocked (classifier flips to `v1.1-dictabert` once the checkpoint lands).

---
**S2 next carry-over:** S3 (DigestScheduler + FCM daily digest) is the next server milestone. Prerequisites: real `IDENTITY_BACKEND=sqlite` db path in `server/.env`; `IDENTITY_DB_PATH` default is `server/data/identity.db`. For Android: the pairing screen + encrypted token storage + Bearer header in `MonitorUploader` is the Android side of S2.

## Files touched this session
- `.claude/agents/android-developer.md` — **created** (fresh project-scoped agent; ~13 KB)
- `.claude/agents/project-manager.md` — **created** (fresh project-scoped agent; ~14 KB)
- `.claude/agents/README.md` — updated (project-scoped 4 → 6; handoff matrix; agent→skill binding; user-global section)
- `.claude/gaps.md` — updated (2026-06-07 status note; agent count 4 → 6 → 10 total; "Agents missing entirely" section flipped to DONE/SUPERSEDED)
- `plan-docs/decisions/android-developer-agent.decision.md` — **created** (D1–D4)
- `plan-docs/decisions/project-manager-agent.decision.md` — **created** (D1–D4 including supersession of gap-analysis "don't create" decision)
- `prompts/2026-06-07_meeting-6.md` — **created** (this file)
- `.claude/skills/README.md` — updated (turn 3: user-global table rows for PM-agent-invoked skills; agent→skill bindings block; 2026-06-07 gap-analysis note)
- `server/app/alerts/fcm_notifier.py` — **rewritten** (skeleton → real FCM delivery; turn 4)
- `server/app/main.py` — updated (`ALERTS_CHANNEL` channel selection in `lifespan()`)
- `server/requirements.txt` — updated (`firebase-admin`, opt-in)
- `server/.env.example` — updated (Alerts section: `ALERTS_CHANNEL` + `FCM_SERVICE_ACCOUNT_PATH` + `ALERTS_` knobs)
- `server/tests/alerts/test_fcm_notifier.py` — **created**; `server/tests/alerts/test_fcm_stub.py` — **deleted**
- `server/app/alerts/__init__.py`, `server/app/alerts/protocol.py` — updated (stale doc comments)
- `plan-docs/decisions/alerts-fcm.decision.md` — **created** (D1–D4)
- `server/app/identity/__init__.py` — **created**
- `server/app/identity/protocol.py` — **created** (IdentityStore Protocol + 3 value types)
- `server/app/identity/sqlite_adapter.py` — **created** (SqliteIdentityStore; 4 tables; WAL)
- `server/app/identity/in_memory_adapter.py` — **created** (InMemoryIdentityStore; fast tests)
- `server/app/identity/settings.py` — **created** (IdentitySettings; IDENTITY_ prefix)
- `server/app/identity/router.py` — **created** (5 endpoints: register/children/pairing-code/pair)
- `server/app/gateway.py` — updated (DeviceAuthMiddleware + _AUTH_ALLOWLIST + step 3b in register_gateway)
- `server/app/monitor/router.py` — updated (_enforce_child_auth; S2 auth docs; removed "No auth in S1")
- `server/app/main.py` — updated (_build_identity + lifespan wiring + identity close + register_identity)
- `server/tests/identity/__init__.py` — **created**
- `server/tests/identity/test_identity_store.py` — **created** (34 tests; parametrized InMemory+SQLite)
- `server/tests/identity/test_auth_middleware.py` — **created** (middleware + monitor auth; full HTTP pairing flow)
- `server/tests/monitor/test_monitor_ingest.py` — updated (2 TestClient tests use pairing flow / disable auth)
- `CLAUDE.md` — updated (status row: S2 shipped, 488 tests)

### 7. Implement S4 Parent review/react API (server side)
**Prompt (1-line restatement):** Build S4 server side per plan `linked-yawning-sifakis.md` — parent review/react endpoints, SQLite as the default flagged store, `set_parent_severity` store method, and export endpoint; keep 516-test suite green.
**Delivered:**
- **`server/app/flagged/settings.py`**: Flipped default `backend` from `"memory"` → `"sqlite"` for durability (parent history survives restart). Tests override via `FLAGGED_BACKEND=memory`.
- **`server/app/flagged/protocol.py`**: Added `set_parent_severity(flag_id, severity)` to the `FlaggedEventStore` Protocol (returns bool; transitions status to `"labeled"`; does not overwrite `parent_label`).
- **`server/app/flagged/in_memory_adapter.py`**: Implemented `set_parent_severity`.
- **`server/app/flagged/sqlite_adapter.py`**: Implemented `set_parent_severity` (SQL `UPDATE SET status='labeled', parent_severity=?`).
- **`server/app/flagged/router.py`** — **created**: `register_parent_review(app)` with 4 endpoints (all parent-authed via `_require_parent` helper; resource ownership enforced in router):
  - `GET /v1/parent/alerts`: query params `child_id?`, `since?`, `status?`, `include_acked?` (default false), `limit?` (1–200, default 50). Aggregates across all parent's children when `child_id` omitted; sort newest-first; post-filters by status. Returns `list[FlaggedEventOut]`.
  - `GET /v1/parent/alerts/{flag_id}`: single event; 404 for unknown or non-owned (ownership-blind 404 to prevent enumeration).
  - `POST /v1/parent/alerts/{flag_id}/react`: `ReactRequest{action, label?, severity?}`. Acknowledge → `status=acknowledged`; label → `parent_label` persisted (DictaBERT training data) + `status=labeled`; severity → `parent_severity` + `status=labeled`. 422 on missing required field. Returns updated `FlaggedEventOut`.
  - `GET /v1/parent/labels/export`: returns all events with `parent_label is not null` as flat dicts (`{quote, label, parent_label, severity, parent_severity, app_package, direction, created_at}`). Docstring notes PII-scrub/retention (S5) applies before real export.
- **`server/app/flagged/__init__.py`**: Exports `register_parent_review`.
- **`server/app/main.py`**: Imports + calls `register_parent_review(app)`. Version bumped `0.6.2-digest` → `0.6.3-parent-review`.
- **`server/tests/parent/__init__.py`** + **`server/tests/parent/test_parent_review.py`** — **created**: 34 new tests:
  - Store-level: `set_parent_severity` persists + returns False on not-found + does not clear existing `parent_label` — parametrized over InMemory+SQLite.
  - Endpoint auth: no token → 401; child-role token → 403 (all 4 endpoints).
  - `GET /alerts`: empty list for no events; returns own children's events; different parent gets empty (no children); `child_id` filter; non-owned `child_id` → 403; newest-first ordering; `include_acked=false` default excludes acked; `include_acked=true` includes them.
  - `GET /alerts/{id}`: happy path with all fields; 404 for unknown; 404 for other parent's flag.
  - `POST /react`: acknowledge sets `status=acknowledged`; label sets `parent_label`; `not_offensive` label; severity sets `parent_severity`; `label` action without `label` field → 422; `severity` action without `severity` field → 422; 404 for unknown; 404 for other parent's flag; label persisted and visible via GET detail.
  - `GET /labels/export`: only labeled events; empty when none; 401 for no token; only own children's events.
- **Test result: 550 passed, 5 skipped** (was 516+5 skipped — net +34 tests).
**Decisions / files of note:**
- `plan-docs/decisions/parent-surface.decision.md` D3 was the pre-existing lock for these actions.
- Ownership-blind 404 (not 403) for non-owned flag detail/react: chosen to prevent enumeration of flag IDs across parents.
- `set_parent_severity` added as a separate method (not folded into `set_parent_label`) to keep the actions orthogonal and the Protocol's semantic clarity intact.

### 8. Build child-mode Android app (A1+A2+A3 in one pass)
**Prompt (1-line restatement):** Implement the child-mode monitoring Android app (milestones A1-A3) per `android_client/design.md` and the frozen server contract: package rename + Gradle flavors, Retrofit/Room/WorkManager/Hilt wiring, AccessibilityService capture engine, PreFilter, MonitorUploader, pairing screen + Bearer auth, consent + permission-flow onboarding.
**Delivered:**
- Package rename `com.dima.offensivehebrew` to `com.shomer.client` via Gradle product flavors (poc + client; both APKs build green). Shared single source tree; old package has tombstone files.
- A1: `build.gradle.kts` (Hilt, WorkManager, Room, security-crypto), `ShomerApplication` (Hilt + 4 notification channels), `NetworkModule` Hilt singleton, `TokenStore` (EncryptedSharedPreferences, Android Keystore), `AuthInterceptor`, `SettingsRepository` (migrated + injected), `MonitorApi` + `PairingApi` Retrofit interfaces, `Models.kt` (all wire types matching server/app/schemas.py).
- A2: `ShomerAccessibilityService` (tree-walking, depth-bounded, package-filtered), `TargetAppRegistry` (4 apps + direction heuristics), `CaptureCoordinator` (400ms debounce, submits to PreFilter + EventDao), `PreFilter` (pure Kotlin, Hebrew ratio test, rolling dedup, sha256), `EventEntity` + `EventDao` (Room, FIFO batch read, delete-on-upload), `CaptureForegroundService` (non-dismissible ongoing notification, START_STICKY), `BootReceiver`, `MonitorUploader` (HiltWorker, 50-event batches, retry/backoff, Bearer auth via AuthInterceptor).
- A3: `ConsentScreen`, `PairingScreen`, `PermissionFlowScreen`, `StatusScreen`, `OnboardingViewModel` (all Hilt); single-Activity `MainActivity` with Compose Navigation (CONSENT->PAIRING->PERMISSIONS->STATUS routing from TokenStore state).
- Parent-mode placeholder at `parent/ParentModePlaceholder.kt` with TODO for pass 2.
- Build: `assembleClientDebug` BUILD SUCCESSFUL, `assemblePocDebug` BUILD SUCCESSFUL. Fixed Kotlin nested block comment bug (bare `/*` in KDoc text).
**Decisions / files of note:** `plan-docs/decisions/android-child-mode-build.decision.md` (D1 flavor strategy, D2 Room vs SQLCipher, D3 Moshi reflection, D4 CaptureCoordinator singleton, D5 KDoc comment rule).

## Open / carry-over to next session (updated after S4)
- **S5 Privacy & hardening** — TLS reverse proxy; at-rest encryption; PII-scrub logs (G-06); rate-limit tuning for monitor volume; confirm `MONITOR_STORE_RAW=false` semantics; consent screen + non-dismissible monitoring indicator (Android).
- **Web dashboard (main thread parallel track)** — reads the now-complete S4 API (`GET /v1/parent/alerts`, `/{id}`, `POST /{id}/react`, `GET /v1/parent/labels/export`).
- **Android parent-mode** — list/detail/react UI wired to the same S4 endpoints; FCM deep-link receiver.
- **S3 digest tests still pending from earlier carry-over** — full HTTP round-trip (not just unit) for the daily digest push.
- Pre-existing carry-overs: DictaBERT training track; populate `server/.env` with real LLM keys.

## Files touched this session (S4 additions)
- `server/app/flagged/settings.py` — updated (default `backend` flipped `"memory"` → `"sqlite"`)
- `server/app/flagged/protocol.py` — updated (`set_parent_severity` added to Protocol)
- `server/app/flagged/in_memory_adapter.py` — updated (`set_parent_severity` implemented)
- `server/app/flagged/sqlite_adapter.py` — updated (`set_parent_severity` implemented)
- `server/app/flagged/router.py` — **created** (4 parent-authed endpoints; `register_parent_review`)
- `server/app/flagged/__init__.py` — updated (exports `register_parent_review`)
- `server/app/main.py` — updated (imports + calls `register_parent_review`; version `0.6.3-parent-review`)
- `server/tests/parent/__init__.py` — **created**
- `server/tests/parent/test_parent_review.py` — **created** (34 tests)

---

## Session close — monitoring app S1→S4 complete + verified end-to-end

(Consolidates what the per-phase agent entries above missed: S3 digest, the web dashboard, the
demo script, and final verification. The four server phases ran as sequential `backend-developer`
background agents, each full-suite-green before the next.)

**S3 daily digest + push** (between turns 6 and 7 above): `server/app/digest/` — `DigestScheduler`
(`AsyncioCronDigestScheduler` ↔ `ManualDigestRunner`) + `DigestStore` (Sqlite+InMemory). Aggregates
each child's day into ONE digest (`GET /v1/parent/digests/{date}`, `POST /internal/digest/run`).
Added FCM-token registration (`PATCH /v1/device/fcm-token` + the S2-deferred `fcm_token` column) and
`flagged.child_ids_with_events_since`. Critical-severity immediate-push via an injected
`on_critical_flag` callback that keeps `MonitorIngest` import-clean of digest/identity concretes.
**516 tests.**

**Final state:** **550 passed, 5 skipped** (full suite 5m55s, 0 regressions). `main.py`
`0.6.3-parent-review`. The complete vertical slice is live and demoable through the real
composition root: child pairs (OTP→device token) → batch-uploads captured messages →
dedup+classify+triage → flag `alerted`/`review_needed` → parent lists + reacts (ack/label/severity)
→ once-a-day digest → parent labels export for DictaBERT training.

**Non-server deliverables this session:**
- `dashboard/index.html` + `dashboard/README.md` — self-contained Hebrew-RTL **parent web dashboard**
  (alerts list w/ borderline review queue, detail + 3 react actions, daily-digest view; localStorage
  base-URL+token; reconciled to the S4 `FlaggedEventOut` bare-array shapes).
- `android_client/design.md` — **Android client LLD** (AccessibilityService capture + ML-Kit OCR
  fallback, on-device pre-filter, encrypted buffer, `MonitorUploader`, parent-mode FCM+react,
  consent/onboarding, 6 milestones). Build not yet started.
- `scripts/monitor_demo.py` — **end-to-end demo** via in-process TestClient (no uvicorn orphan),
  deterministic classifier stub. Verified: `accepted=3 deduped=1 flagged=2` + full parent loop. Green.

**Open / carry-over (authoritative, supersedes the duplicated sections above):**
1. **Android build** (next big lift) — implement `android_client/design.md` via the `android-developer`
   agent: package rename `com.dima.offensivehebrew → com.shomer.client` + flavors; AccessibilityService
   capture engine; `MonitorUploader`; pairing + Bearer auth; parent-mode FCM digest + react UI;
   consent/onboarding. Depends on nothing further server-side (S1–S4 contract is frozen + tested).
2. **S5 privacy hardening** — TLS reverse proxy + cleartext-off prod host; at-rest encryption;
   PII-scrub logs (G-06); **enforce `MONITOR_STORE_RAW=false`** (classify-and-discard — currently the
   raw text is still persisted for monitor events; the setting exists but the non-flagged-event
   blanking is the remaining work); rate-limit tuning for monitor volume; consent screen + indicator.
3. **S6 scale + classifier gate** — Redis dedup adapter; async ingest queue; batched DictaBERT
   inference; build a **monitor-realistic eval slice** and gate the `v1.1-dictabert` flip on
   `hate`/`violence` recall + calibration (not just F1≥0.78). Owner: `ai-researcher-developer`.
4. **Ops for live FCM** — a real Firebase project + service-account JSON before a device-push demo
   (digest delivery currently logs via `LogNotifier`; `FcmNotifier` is implemented, needs creds).
5. Pre-existing: DictaBERT training track; populate `server/.env` with real LLM keys.

**Files touched (S1→S4 + non-server, consolidated):**
- Server new packages: `server/app/{dedup,flagged,monitor,identity,digest}/` (each `protocol.py`
  + adapters + `settings.py` + `router.py` where applicable + `__init__.py`).
- Server modified: `server/app/schemas.py` (monitor value types), `server/app/main.py` (lifespan
  wiring for all new ports + routers; `0.6.3-parent-review`), `server/app/gateway.py`
  (`DeviceAuthMiddleware` + allowlist).
- Server tests: `server/tests/{dedup,flagged,monitor,identity,digest,parent}/` (~+163 tests; 387→550).
- Decisions: `plan-docs/decisions/{monitor-architecture,privacy,parent-surface}.decision.md`.
- Client/docs/demo: `dashboard/index.html`, `dashboard/README.md`, `android_client/design.md`,
  `scripts/monitor_demo.py`.
- Plan: `~/.claude/plans/linked-yawning-sifakis.md`.

### 9. Build parent-mode Android app (A4 — pass 2 of 2)
**Prompt (1-line restatement):** Implement Android parent-mode (milestone A4) per `android_client/design.md §4+§9` and frozen server contract (550 tests); child-mode pass 1 already compiles; match field names exactly; FCM must be opt-in so a clean build requires no Firebase project.
**Delivered:**
- **`data/Models.kt`** extended: `FlaggedEventOut` (14 fields, all `@Json(name=...)` snake_case), `ReactRequest`, `ChildDigestOut`, `DigestEntryOut`, `ParentRegisterRequest`, `ParentRegisterResponse`, `ChildInfo`.
- **`data/ParentApi.kt`** (new Retrofit interface): `registerParent`, `listChildren`, `setFcmToken`, `listAlerts` (4 nullable @Query params), `getAlert`, `react`, `getDigests`.
- **`di/NetworkModule.kt`** extended: `provideParentApi` singleton added.
- **`data/TokenStore.kt`** extended: `saveParentToken(parentToken, parentId)`, `getParentId()`, `ROLE_CHILD`/`ROLE_PARENT` constants.
- **`MainActivity.kt`** rewritten: routes on `role` (null → ROLE_CHOOSER; parent → PARENT_AUTH or ALERT_LIST; child → existing consent/pairing/status flow). Nav graph extended with ROLE_CHOOSER, PARENT_AUTH, ALERT_LIST, ALERT_DETAIL (with `flag_id` nav arg), DIGEST routes.
- **`parent/RoleChooserScreen.kt`** (new): top-level "I am a Parent / This is my child's device" branching shown on first launch.
- **`parent/ParentAuthScreen.kt` + `ParentAuthViewModel.kt`** (new): two-tab UI — create new account (POST /v1/parent/register) or paste existing opaque token. Stores via `TokenStore.saveParentToken`. `@HiltViewModel`.
- **`parent/AlertListScreen.kt` + `AlertListViewModel.kt`** (new): poll-based list with 4 filter chips (All / Needs Review / Alerted / Done); `review_needed` visually prominent (error container color + bold chip). Pull-to-refresh via `PullToRefreshBox`. Optional child filter row. `@HiltViewModel`.
- **`parent/AlertDetailScreen.kt` + `AlertDetailViewModel.kt`** (new): metadata card + quote card + explanation card + 3 react groups (acknowledge, label offensive/not_offensive, severity low/medium/high/critical). `SavedStateHandle` for `flag_id`. On react success → pop back. `@HiltViewModel`.
- **`parent/DigestScreen.kt` + `DigestViewModel.kt`** (new): date-picker arrows (step ±1 day), `GET /v1/parent/digests/{date}`, 404 → informational "no digest yet" panel (not error). Shows total_flagged, review_needed, by_severity, by_label, per-entry list.
- **`parent/ShomerFcmService.kt`** (new): FCM strategy — service is written in full (onNewToken→PATCH /v1/device/fcm-token; onMessageReceived→parse data payload→post notification→deep-link to AlertList/Digest) but is **commented out and not registered in the Manifest**. The file compiles to a no-op placeholder object. See "FCM strategy" section below.
- **Both flavors compile green:** `assembleClientDebug` BUILD SUCCESSFUL; `assemblePocDebug` BUILD SUCCESSFUL. No Firebase project needed.
**Decisions / files of note:** `plan-docs/decisions/android-parent-mode-build.decision.md` (D1 FCM opt-in strategy; D2 opaque-token parent auth; D3 polling-first, FCM additive).

---

## Addendum — alert-delivery channels (FCM hardening + free ntfy.sh push)

(Follow-up after turn 4's FcmNotifier: "did you test it?" → coverage hardening; then
"does FCM cost money / easier options?" → added a free push channel.)

- **FcmNotifier test hardening.** Turn-4's run was green but only 76% covered the new file — the
  *real* Firebase branch (init / build `messaging.Message` / `messaging.send`) never ran because
  `firebase-admin` isn't installed. Added tests injecting a **stand-in `firebase_admin`** module
  (via `monkeypatch` + `sys.modules`): real-resolution success (asserts the 9-field payload,
  `deep_link`, priority, channel_id, single idempotent `initialize_app`), bad-creds→degrade,
  real-path send-failure→queued, present-but-no-creds, audit-failure-swallowed, top-level-raise.
  **`fcm_notifier.py` now 100% line-covered.** Honest caveat: real over-the-wire delivery to
  Google FCM still needs a service account + device (out of server scope).
- **Cost answer:** FCM itself is **$0** (no per-message charge); the cost is *setup* — a Firebase
  service account + the Android receiver + a Play-services device — and it's blocked on the unbuilt
  client. User chose **web dashboard (pull) + ntfy.sh (push)**.
- **`NtfyNotifier` (new free push channel, `ALERTS_CHANNEL=ntfy`).** Publishes to ntfy.sh via the
  **JSON** endpoint (UTF-8 → Hebrew-safe titles) over the already-present `httpx` (**no new dep**).
  Same contract as the other channels: idempotency → not-configured degrade (empty topic) →
  rate-limit → POST with exponential-backoff retry → `LocalRetryQueue` fallback → audit; never
  raises. Severity→ntfy priority (low2…critical5); tags `[label,severity]`; optional Bearer token +
  `click` (dashboard) URL. ntfy config added to `AlertSettings` (`ALERTS_NTFY_*`); exported in
  `__init__`; `main.py` `lifespan()` gained the `ntfy` branch; `.env.example` Alerts section
  rewritten (ntfy + fcm). **13 ntfy tests via `respx`, 100% line-covered.**
- **Dashboard pull-path verified** (the other chosen option, needs no code): `register_parent_review`
  is wired in `main.py`, flagged store constructed, and `dashboard/index.html` calls
  `/v1/parent/alerts`, `/{id}`, `/digests/{date}`, `/register`. Confirmed working.
- **Alerts suite: 129 passed** (was 116). Decision: `plan-docs/decisions/alerts-delivery-channel.decision.md`
  (D1 dashboard+ntfy near-term over FCM-first; D2 JSON publish + httpx, no new dep).
- Files: `server/app/alerts/ntfy_notifier.py` **(new)**, `server/tests/alerts/test_ntfy_notifier.py`
  **(new)**, `server/app/alerts/{settings,__init__}.py`, `server/app/main.py` (ntfy branch),
  `server/.env.example`, `server/tests/alerts/test_fcm_notifier.py` (real-path tests),
  `plan-docs/decisions/{alerts-fcm,alerts-delivery-channel}.decision.md`.

### (cont.) Manual testing documentation for the Android app + server
**Prompt:** "create documentation for manual testing of the android app with server".
**Delivered:**
- `android_client/MANUAL_TESTING.md` — hands-on QA guide: prerequisites, start server (`--host 0.0.0.0`,
  firewall), build/install the `client` flavor (APK-uninstall caveat), mint a pairing code, then
  numbered test cases TC1–TC9 (connectivity · consent · pairing · permissions · capture+upload ·
  flag-reached-server · parent review/react · daily digest · dedup) each with steps + expected results +
  checkboxes, plus negative/security checks, pass criteria, a troubleshooting table, and the by-design
  "known limitations" (stand-in classifier, manual digest, LogNotifier, MONITOR_STORE_RAW pending).
- `scripts/make_pairing_code.py` — one-command helper that registers a parent + child + prints a 6-digit
  pairing OTP + parent token (removes the only manual-test friction point — there's no create-child UI).
**Files:** `android_client/MANUAL_TESTING.md` (new), `scripts/make_pairing_code.py` (new).

### (cont.) Integration + manual testing guide for the full push/alert flow
**Prompt:** "we need to do integration for the full flow containing push notification and alerts — integration and manual testing flows — prepare guide".
**Delivered:**
- Mapped the **3 push trigger points** (verified in code): (1) **critical-immediate** — `MonitorIngest`
  step 6 fires `on_critical_flag → send_alert(message_id="critical:…")` when a flagged event is
  `status="alerted"` AND severity ∈ {high, critical}; (2) **daily digest** — `DigestScheduler.deliver
  → send_alert(message_id="digest:{child}:{date}")` via `POST /internal/digest/run`; (3) legacy
  `/classify` ALERT_DIRECT. Key gotcha documented: `abusive` caps at **medium** severity → does NOT
  fire the immediate push (use `pornographic`/`hate` ≥0.70). All three paths use the lifespan
  `notifier` instance (captured in closures) → selected via `ALERTS_CHANNEL`.
- **`integration/integration-push-alerts.md`** (new) — full guide: trigger map + severity table +
  channel matrix (log/ntfy/fcm/stub; ntfy needs no per-device token, fcm does), Part A automated
  (A1 new push test, A2 alerts suite, A3 `monitor_demo`, A4 `/classify`), Part B manual (B0 ntfy
  smoke, B1 ntfy-through-pipeline incl. the `$env:ALERTS_CHANNEL="ntfy"` + `monitor_demo` digest-push
  trick, B2 LogNotifier, B3 FCM, B4 dashboard, B5 rate-limit/dedup/idempotency), checklist,
  troubleshooting, known limitations.
- **`server/tests/integration/test_push_alert_flow.py`** (new, 2 tests) — through the real
  composition root with `ALERTS_CHANNEL=stub` + a keyword classifier: asserts the critical-immediate
  push (`pornographic`→high) and the digest push both fire and are captured on the `StubNotifier`;
  benign-only batch fires nothing. **Both pass.** (Fills the gap: `test_full_flow.py` only covered
  the `/classify` path, not the monitor critical/digest pushes.)
- **`scripts/test_ntfy.py`** (new) — classifier-independent ntfy smoke test; reads `ALERTS_NTFY_*`
  from `server/.env`, publishes one sample Hebrew alert. **Verified live** — real `HTTP 200` + message
  id from ntfy.sh. UTF-8 stdio fix added (cp1252 console can't print Hebrew/icons).
- **Tests: integration + alerts = 138 passed** together (no env leakage; monkeypatch fixture).
**Files:** `integration/integration-push-alerts.md` (new), `server/tests/integration/test_push_alert_flow.py`
(new), `scripts/test_ntfy.py` (new).
