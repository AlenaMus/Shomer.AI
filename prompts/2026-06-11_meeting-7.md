# Session prompts log — 2026-06-11 (toward Meeting 7)

### 1. Remaining-tasks audit for demo + presentation readiness

**Prompt (restated):** Use the Hebrew project-manager agent to go over the whole project and
produce a list of remaining tasks to complete end-to-end functionality, be ready for a full
demo, and create the presentation + results.

**Delivered:** `docs/remaining-tasks-demo-readiness.md` (Hebrew RTL) — full gap analysis +
remaining-tasks plan, grouped into (א) E2E functionality gaps, (ב) research-question/results
work, (ג) demo preparation, (ד) presentation + results deliverables; each task with owner
agent, effort (S/M/L), and demo-blocker vs. nice-to-have priority, plus a 3-step critical
path (unblock demo ~1 day → scientific integrity ~1 week → presentation ~1 week).

**Key findings surfaced:**
- Demo blockers: on-device Android integration test (never run live), LLM-key verification,
  `MONITOR_STORE_RAW=false` enforcement, fallback demo script, structured demo scenario.
- Context-FP MVP (2026-06-11, 61 items): F1 product-level PASSES H1 (ΔFPR −29.4pp, p=0.002)
  but misses recall non-inferiority; F2 prompt-level does NOT pass H1 (baseline FPR=0% — a
  finding, not a failure). Needs an analysis memo + Alona's decision on which is primary.
- Scientific must-haves: gold set v2 (61→150-200 + double annotation for κ≥0.6), honest
  real-only eval, limitations section.

**Open decisions for Alona:** (1) F1-primary vs. wait for larger gold set; (2) gold-set size
before defense; (3) availability of a second annotator for Cohen's κ.

### 2. On-device integration test — Android client → server, text + screenshot/OCR (PASSED)

**Prompt (restated):** Test the Android client → server full flow including screenshots for
OCR validation and text messages; check the server is running and receiving.

**Delivered (live test on the Huawei P20, `com.shomer.client`, server `:8011`):**
- Diagnosed why 8 captured events were stuck "pending": the app's Base URL pointed at a
  stale address (`192.168.1.131:8021`); PC is `192.168.68.100`, server on `:8011`,
  `adb reverse tcp:8011` active. Fix = set Base URL to `http://127.0.0.1:8011/` on the
  phone. Pairing was still valid (child `35c62fce…` token live in `identity.db`) — no
  re-pair needed.
- **Text path PASS:** 8 WhatsApp messages uploaded (`batch_processed accepted=8 flagged=1`).
- **Screenshot/OCR path PASS (first-ever live run):** 3 real screenshots received →
  Tesseract OCR (heb+eng) → segmented 15/33/21 lines → per-line DictaBERT + Gemini CA →
  4 screenshot-sourced parent alerts incl. a `violence` flag on a real bullying line.
  This validates the 2026-06-11 per-line segmentation fix on-device.
- Marks "on-device integration test" (demo-blocker #1 in `docs/remaining-tasks-demo-readiness.md`
  and Next-session priority #1) as DONE for the happy path.

**Known issues observed (logged, not failures):**
- `request_timeout` (30 s) on `/v1/monitor/image` for dense screenshots (30+ OCR lines ×
  ~1.3 s CA calls); server completes after timeout, client retry gets deduped, but the
  app-side counter can undercount → fix via longer timeout or async ingest (S6).
- Calibration over-escalation flagged several benign lines (known caveat, tracked).

### 3. F1/F2 explained + Context Agent context-source deep-dive

**Prompt (restated):** Explain the F1/F2 split, FPR/recall meanings; then — would training
DictaBERT on full conversations reduce FP, and where does the Context Agent actually get its
conversation context from (OCR vs. single message)?

**Delivered (explanations, no code):**
- F1 = product-level A/B (`CONTEXT_AGENT_ENABLED` off/on, whole pipeline); F2 = prompt-level
  A/B (same LLM judge, history block empty/populated — the scientific primary). Naming
  collision with F1-score flagged — rename in thesis.
- Training DictaBERT on conversations = "naive concatenation" = experiment E6; Pavlopoulos
  2020 found marginal gains; no Hebrew conversational data exists → recommended as E6
  comparison or Future Work, not a D10 retrain.
- Code-level finding: CA history comes from the server's `conversations` table (last 5 turns
  per child), populated by `_persist_turn` on every event — screenshot OCR lines polluted it,
  all turns hardcoded `role="child_outbound"`, screenshot context only seen "by accident."

### 4. Context-source routing implemented (OCR → screenshot conversation; text → DB history)

**Prompt (restated):** Make the CA use the whole conversation from the OCR image for
screenshot events, and the per-child history only for single text messages.

**Delivered (backend-developer agent):** `evaluate(provided_history=...)` on the
`ContextReasoner` protocol + agent (skips `read_history`, audit shows `"provided_history"`);
`_run_pipeline(conversation_turns=...)` passes it through and skips `_persist_turn` for
screenshot events; `ingest_batch`/`_process_event` thread the kwarg; image endpoint builds
`role="screenshot_line"` turns from the full ordered segment list. 7 new tests.
**590 passed, 8 skipped — zero regressions.**
Decision: `plan-docs/decisions/context-source-routing.decision.md`.

## Open / carry-over
- Execute critical-path Step 1 (demo unblock): verify keys → smoke → ~~on-device test~~ (DONE 2026-06-11) → raw-store hardening.
- Rerun the context-FP F1 eval after the context-source routing change (may move Category-C recall).
- Real message direction (inbound/outbound roles) still not threaded into conversation turns — known limitation.
- Alona's three decisions above gate the presentation narrative.
- `/v1/monitor/image` 30 s request timeout on dense screenshots → async ingest or longer timeout (S6).
- `adb reverse` does not survive USB unplug/reboot — re-run before the next phone session, or switch Base URL to `http://192.168.68.100:8011/` + firewall rule.

## Files touched
- `docs/remaining-tasks-demo-readiness.md` (new, by project-manager agent)
- `prompts/2026-06-11_meeting-7.md` (this file)
- `test-sessions/phone_screenshot_watch.log` (transient logcat watch, this session)
- `server/app/context_agent/{protocol,agent}.py` · `server/app/main.py` · `server/app/monitor/{ingest,router}.py` (context-source routing)
- `server/tests/context_agent/test_provided_history.py` · `server/tests/monitor/test_monitor_image_context_routing.py` (new)
- `server/tests/{monitor/test_monitor_ingest,digest/test_critical_immediate,parent/test_parent_review}.py` (stub `**kwargs`)
- `plan-docs/decisions/context-source-routing.decision.md` (new)
