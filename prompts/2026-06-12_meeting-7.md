# Session prompts — 2026-06-12 (toward Meeting 7)

### 1. Parent dashboard: URL + username/password login + per-child separation

**Prompt (restated):** Use web UI/UX developers to rework the parent dashboard: accessible at a
URL, login with username+password, full separation of alerts by choosing a child *by name*
(show only that child's data), English "Shomer.AI" logo, login centered on the page. Work on a
separate branch until merge is approved.

**Delivered (branch `feature/parent-dashboard-login`):**
- Pre-existing uncommitted work from 2026-06-08..11 sessions checkpointed on `main`
  (`734d9fc`, 119 files) so the feature branch is clean.
- **Backend** (`backend-developer` agent): `POST /v1/parent/login` (PBKDF2-SHA256 stdlib
  hashing, same opaque parent-token returned), `register` extended with optional
  username/password (Android back-compat kept), idempotent identity-DB migration, both
  identity adapters updated, gatekeeper allowlist (`/v1/parent/login`, `/dashboard*`, `/`),
  dashboard mounted at **`/dashboard/`** + `GET /` redirect. 27 new tests; full suite
  **648 passed / 8 skipped** from repo root.
- **Frontend** (web-UI agent): `dashboard/index.html` redesigned — centered login/register card,
  LTR **Shomer.AI** wordmark, mandatory child-name chip selector (no free-text child-id, no
  all-children view; selection persisted per parent), 401 anywhere → logout, same-origin base
  URL default, polished responsive dark theme; `dashboard/README.md` rewritten.
- **Verified:** in-process smoke `scripts/_smoke_dashboard_login.py` — 13/13 PASS
  (URL serve, register, dup-409, login good/bad, children by name, own-child 200 /
  foreign-child 403). Dev creds seeded in `server/data/identity.db`:
  `smoke.parent` / `s3cretpass` (child "דנה").

**Decisions:** `plan-docs/decisions/parent-dashboard-login.decision.md`
(token reuse over JWT, PBKDF2 over bcrypt dep, mandatory child selection, FastAPI StaticFiles).

---

### 1b. First-time onboarding: email+password login + OTP-by-email pairing + design diagrams

**Prompt (restated):** Handle first-time onboarding — after the parent registers they should
receive the pairing OTP **by email**, use it in the child app to pair, and log into the dashboard
with **email + password**. (Follow-ups: Gmail-API OAuth alert sending is being implemented by a
different agent; add these flows to the system design diagrams and explain them.)

**Delivered (same branch, supersedes the username variant from §1):**
- **Backend:** login identifier = email (idempotent migration, unique index, uniform 401);
  new `EmailSender` port `server/app/mailer/` (LogEmailSender default + SmtpEmailSender,
  `MAILER_BACKEND`/`SMTP_*` in `.env.example`); `register` accepts `child_name` → creates
  parent+child+OTP in one call, emails the OTP (Hebrew, code also in response for demo);
  `/v1/parent/pairing-code` now also emails. Full suite **660 passed / 8 skipped**.
- **Dashboard:** email login; registration → onboarding wizard (code sent to ⟨email⟩, big copyable
  code + expiry countdown + 3-step child-app guide); in-app "הוסף ילד/ה" + per-child "קוד התאמה"
  modals; email shown in user menu.
- **Verified end-to-end:** smoke 21/21 — register → OTP email captured → `/v1/pair` →
  device token → child visible to parent.
- **Design doc updated** (`docs/design/architecture_diagrams.md`): now 5 diagrams — added
  **Diagram 4** (onboarding sequence: register → OTP email → pair → login) and **Diagram 5**
  (auth + per-child separation enforcement layers); Diagram 1 parent side updated to
  Web-Dashboard + email channel (child-only Android per D-CO-1/D-CO-2).
- **Decision:** D2 appended to `plan-docs/decisions/parent-dashboard-login.decision.md`.
- Coordination note: Gmail-API OAuth **alert** sender (`GmailApiNotifier`, `ALERTS_CHANNEL=email`)
  landed from a parallel session (`child-only-and-email-alerts.decision.md`) — separate port from
  the OTP mailer; both compose; committed together on this branch.

---

### 2. Presentation: explain the RQ, results, architecture, literature + plan the real experiment

**Prompt (restated):** Explain the research question — what's been done, what must be proven and
how — and how to present the server architecture + results to prove it, plus the literature
flagship; produce a short clear presentation summary. Follow-ups: why hasn't the context experiment
run / what's needed; account for the 61-conversation test; fix the presentation data; what's needed
for the real experiment; what conversation groups in % for a good eval; create a data-collection
plan file; verify a clear, simple presentation explanation exists with a glossary + meaningful graphs.

**Delivered:**
- **Correction logged:** my first pass wrongly said the context experiment "hasn't run." It *has* —
  found `docs/research_question/context_fp_mvp_results.md` (61 items) + `harm_context_results.md`
  (143 items). Reread + grounded everything in `context-fp-experiment.decision.md` (D-CFP-1..9) and
  `context_fp_test_plan.md`.
- **`docs/research_question/presentation_summary.md`** — slide-ready RQ/architecture/results/
  literature/contribution summary with the **real** numbers (FPR 55.9→26.5, −29.4pp, p=0.002;
  harm-recall 58.9→82.1, +23pp; veiled threats 0→100%) + honest MVP caveats.
- **`docs/research_question/presentation_explained.md`** — plain-language narrative + **glossary of
  every result term** + embedded graph list (all files verified to exist).
- **`docs/research_question/gold_set_collection_plan.md`** — executable plan for the real gold set:
  reachability check, A/B/C group %s (45/25/30), item schema, double-annotation + κ, ethics,
  effort/go-no-go. (User will decide feasibility, then trigger `scripts/eval_context_fp.py`.)
- **New money graph** `docs/research_question/plots/contribution_two_axes.png` (+`scripts/viz_contribution_summary.py`)
  — single two-panel figure: context cuts false alarms (left) AND recovers hidden harm (right).

**Key framing clarified for the user:** current results answer the *foundational* sub-question (RQ1,
classifier strong) + prove the context mechanism at **MVP/feasibility level**; the single remaining
blocker for a *publishable* magnitude is a **real, double-annotated Hebrew conversational gold set**
(apparatus/runner/stats/thresholds all already built).

---

### 3. Hebrew (RTL) presentation deck — `.pptx`

**Prompt (restated):** Build an actual Hebrew `.pptx`. Slides: title (name + picture), thanks
(Dr. Yoram Segal + husband Dima Shalaev), the problem, the solution/profit, how the system works
(high-level Android+server diagram incl. OCR/CA/DictaBERT/AlertManager/Triage), dataset
(what/amount/%/sources), research question, test + model-training results with explanations,
conclusions, future work. Use commercial-strategist for the commercial slides.

**Delivered — `docs/presentation/Shomer_AI_presentation.pptx` (11 slides) + `.pdf` export:**
- Built with **pptxgenjs** (`scripts/build_deck.js`), full **RTL** (`rtlMode`+right-align, Arial for
  Hebrew glyph coverage), guardian palette (navy/teal/mint).
- **Commercial slides** (problem · solution+value · conclusions/future framing) authored by the
  **commercial-strategist** agent (Hebrew); technical slides (architecture, dataset, RQ, results)
  authored from the verified docs.
- **Visual assets generated** (`scripts/viz_deck_assets.py`): `assets/hero_shield.png` (title hero),
  `assets/dataset_classes.png` (5-class donut), `assets/dataset_sources.png` (provenance bars) — all
  English/numeric labels so no Hebrew-in-image font risk. Reused existing result graphs
  (`contribution_two_axes`, `ollama_vs_dictabert_f1`, `03_per_class_metrics`).
- **Architecture diagram** drawn natively with pptxgenjs shapes: child(Android)→FastAPI server
  [OCR · DictaBERT Classifier · Triage · Context Agent · Alert Manager]→parent, RTL flow.
- **Dataset numbers verified from the actual jsonl** (train 7,974 / val 888 / test 445;
  ~57% real Hebrew / ~36% Gemini-synth / ~7% translated).
- **QA loop done:** rendered via LibreOffice→PDF→JPG, fresh-eyes subagent QA found a **systemic
  title-accent overlap on 8/11 slides** + heart-emoji overlap + 2 chart-overlap issues + minors →
  **all fixed and re-verified** (removed under-title accent per skill guidance, moved motif to a
  left vertical bar; repositioned the +23pp annotation; re-spaced dataset charts).

---

### 4. Presentation deck — major redesign round (English headers, bold terms, fixed architecture)

**Prompt (restated):** Improve the deck — AI terms in English (Context Agent etc.), English headers,
high-res graphs (+ more slides + clear English graph explanations), connect RQ to architecture, add
why-DictaBERT (not LLM/Ollama), bold all concepts/terms, results bold in a different color, fix ALL
architecture arrows + add child/parent flow explanations, parent client = Web App + Gmail, more
professional/modern design.

**Delivered — deck grown 11→13 slides, full rebuild:**
- **Graphs regenerated high-res** (200–220 DPI, large English labels): `contribution_two_axes.png`,
  dataset donut/bars, + **new** `assets/model_quality.png` (DictaBERT vs Ollama macro-F1 + per-class F1).
- **English headers** (Georgia) + small Hebrew gloss on every slide; **footer** with brand + page #.
- **Markup-driven bold**: a `**concept**` / `[[result]]` parser → English terms bold (navy), result
  numbers bold + green (`RESULT`). Manual RTL bullet/number markers (pptxgenjs auto-bullets rendered
  inconsistently on multi-run RTL paragraphs — switched to reliable right-side manual markers).
- **New slides:** *Child & Parent Flows* (numbered steps, both flows) · *Research Question → Architecture*
  (maps context-blind=DictaBERT vs context-aware=+Context Agent; the `CONTEXT_AGENT_ENABLED` switch) ·
  *Why DictaBERT?* (color-coded comparison table vs General LLM vs Ollama + justification bullets).
- **Architecture corrected:** proper pipeline order OCR→DictaBERT→Triage→Context Agent→Alert with
  correct L→R arrows; **Parent Client = Web App + Gmail (Email)**; child upload (HTTPS) + alerts arrows.
- **Glossary strips** (English) on both results slides (macro-F1, ECE, FPR, recall, p-value).
- **Two QA rounds** via fresh-eyes subagents → fixed systemic title-accent overlap, orphan/wrong-side
  bullets, RTL minus-sign glitch (`−29.4`→reworded), clipped arrow label, chart overlaps. Re-verified
  slides 3/5/6/8/11 clean.
- Build: `scripts/build_deck.js` + `scripts/viz_deck_assets.py` + `scripts/viz_contribution_summary.py`.

---

### 5. Presentation deck — content/clarity refinements round

**Prompt (restated):** Clarify "−29 pts vs what"; add how 5-class classification relates to Triage,
when the Context Agent triggers, and when an alert is sent (as flow-discovery); make slide descriptions
full sentences under the header (not bullets); make property bullets keyword-led (Keyword — explanation);
keep comparisons in tables.

**Delivered (deck now 14 slides):**
- **"−29 pts" clarified** → Solution stat card now reads **55.9 → 26.5%** with label
  "context-aware מול context-blind" (answers "vs what").
- **New slide 7 — Decision Logic** ("5 Classes → Triage → Context Agent → Alert"): a color-coded
  **routing table** (Classifier output · Triage decision · Context Agent? · Outcome) encoding the G-03
  rules (≤0.3 silent · abusive/hate ≥0.7 direct · pornographic always · violence always→CA · borderline
  0.3–0.7→CA), + keyword bullets stating exactly when CA triggers and when an alert is sent.
- **Header descriptions = full sentences** (new `header(en, descSentence)`, right-aligned RTL) on every
  content slide instead of short glosses.
- **Property bullets keyword-led** on Solution (Real-time / Context-aware / Privacy-by-design /
  Hebrew-native / Low false-alarms) and Why-DictaBERT (Hebrew-native / Accuracy / Speed / Privacy /
  Low cost / Right-tool-per-job) — `**Keyword** — explanation`.
- **Comparisons kept as tables** (Why-DictaBERT criteria table + the new Decision-Logic routing table).
- Footers renumbered 1→14; final fresh-eyes subagent QA = no overlaps / no garbling / tables fit /
  descriptions clear.

---

## Open / carry-over

- **User approval → merge** `feature/parent-dashboard-login` into `main`.
- Login hardening deferred to S5: attempt rate-limiting/lockout, password reset, TLS, argon2id.
- Android parent mode could adopt the same `/v1/parent/login`.

## Files touched

- `server/app/identity/{protocol,router,sqlite_adapter,in_memory_adapter,__init__}.py`,
  `server/app/identity/passwords.py` (new), `server/app/gateway.py`, `server/app/main.py`
- `server/tests/identity/test_parent_credentials_contract.py` (new),
  `server/tests/identity/test_parent_login.py` (new)
- `dashboard/index.html`, `dashboard/README.md`
- `scripts/_smoke_dashboard_login.py` (new)
- `plan-docs/decisions/parent-dashboard-login.decision.md` (new), this log
